# sPA ss13 — progress log (phantom-codegen-nominal-stdlib)

Branch `spa/ss13` · worktree `../scrml-spa-ss13` · base `e8a5491f` (origin/main).
Append-only. Land per-item; PA re-integrates.

---

## Boot (S209, 2026-06-20)
- Worktree created from origin/main @ e8a5491f; node_modules symlinked (main + compiler); `git status` clean.
- Read: spa.md → spa-scrml.md (contract) → INDEX → ss13 list → core files (SPEC-INDEX §19/§23/§40/§58/§59
  rows, the entry test, representative stdlib `.scrml`, known-gaps lines).
- 5 items. Verified each against PRIMARY sources (live code/tests, known-gaps, §59 RATIFIED-DESIGN, the entry
  test's own deferral notes) per R4/R26 — the S208 footprints proved stale on 2 of 5.

## Outcome — WHOLE LIST PARK/ESCALATE (zero dev-dispatches, zero branch landings)
ss13 bundled two stale premises: "Nominal-flip green-field §29/§58/§59" (but §59 is BUILT; §29/§58 are
deliberate deferrals) and "stdlib mechanical cleanup" (but Phase 3 is design-gated). None is bounded
sПА-executable. Per spa-scrml.md §Boundaries: "If the whole list stalls on escalations, report to the PA and
stand down — don't force progress." Doing exactly that. Dispositions below.

## Item 1 — stdlib-canonical-form-cleanup  [PARKED → PA scope (design-gated)]
The entry test `compiler/tests/unit/stdlib-canonical-form-cleanup.test.js` is the tell: Phase 1 (E-EQ-004,
`===`/`!==`) is DONE + green; Phase 3 (C21/C22/C23) is **deliberately `.skip`'d** with the authored note
"the cleanup requires coordinated API refactor (signatures + callers + spec extension) … out of scope for this
dispatch. PA review will scope a follow-up." Verified the live surface (counts incl. comments):
- **3a `throw new Error/TypeError/RangeError`** — 43 hits / 11 files (auth, crypto, http, oauth×5, test,
  meta-checker). Converting → `fail .Variant` makes each fn FAILABLE → changes its contract → every caller must
  handle the failure. The `scrml:test` assertion fns (`assertEqual`/`assertTruthy`/`assertThrows`/…) are called
  from every `~{}` inline test block in the corpus; `assertThrows` *itself* uses `try{ fn() }catch` to catch a
  throw from the function-under-test. Blast radius ⟶ far beyond the list's stdlib + api.js + module-resolver
  shared ingestion. (R2: not a mechanical rewrite; would break the assertion framework if done naively.)
- **3b `try{}`** — 9 hits / 5 files (test, data/validate, fs, http, meta-checker). Same `!{}`-semantics
  question: what does `!{}` mean wrapping a host-JS call that may throw, and how does it compose with the
  assertion fns that depend on catching? Undefined without a §19-fail / §23-foreign-code model.
- **3c `from "bun"` / `"bun:sqlite"`** — 2 files (redis, store/kv). Test's own note: **"needs SPEC §40.4
  amendment OR vendoring."** That is a design ruling (Boundaries: "an item needs a SPEC amendment / a ruling").
- R4 note on the `${…}` blocks: §23 row confirms `_{}` is the foreign-code passthrough context; the stdlib
  files use `${…}` (scrml **logic**), so `throw`/`try` ARE scrml-surface anti-patterns (not legit host-JS) —
  the migration is "real," but the model (how `fail`/`!{}` cross the host boundary + caller contracts) is undefined.
- **No code landed.** ESCALATE: PA/dPA scope a `fail`/`!{}`/bun-import migration model (incl. the §40.4 ruling)
  before any dispatch. The test's Phase-3 `.skip`s stay skipped until then.

## Item 2 — browser-language-overclaims (§23)  [PARKED — user-blocked]
§23 sidecar / WASM / supervised-restarts are spec-defined with zero codegen (grep: 0 `WebAssembly.instantiate`,
0 sidecar-spawn). The only available action is amending published articles that overclaim these as live — and
the user verbatim said **"no amendments to published articles for now."** So the only lever is vetoed. The
phantom-codegen gap stands as a record; no change landed.

## Item 3 — vanilla-file-interop-impl (§29)  [PARKED — friction-gated deferral]
known-gaps Bug 10 (`§29 vanilla-interop … Nominal / framing-corrected S132`): the false present-tense
pass-through claim was removed S132; §29 is KEPT in SPEC as Nominal/spec-ahead, NOT retired, with an explicit
**re-trigger ≥2 adopter friction reports** gate. The feature is deliberately NOT implemented; live JS interop
today is via §21 import (distinct + working). No friction trigger has fired. This is a ratified deferral, not
undone work — PARK. No change landed.

## Item 4 — build-story-impl (§58)  [PARKED → re-bucket to design/feature track]
known-gaps:1236, filed by **sibling sPA ss14 item5 (S209)**: "§58 Build Story is SPEC'd but NOT implemented
(0 impl symbols in `compiler/src`) … Re-bucket candidate: a deferred feature arc, not an sPA fix — consider
moving to a design/feature track." Verified independently: 0 `buildStory`/`merkle`/`build-story.lock` symbols
in `compiler/src` (only generic `compute-program-config.ts` + `commands/build.js`). known-gaps Nominal-1
estimates ~90-200h, M6-gated; §58.12 flags `*`-marked not-yet-proven clauses; the feature is trust-sensitive
(content-addressed Merkle closure + SHA-256 + lock sidecar = supply-chain surface). A speculative unsupervised
sПА first-slice on a trust feature is the wrong call (R2/R3). CONCUR with ss14 — re-bucket to PA/dPA staging.
No change landed.

## Item 5 — value-native-maps-impl (§59)  [PARKED → ALREADY BUILT; list footprint stale]
**The footprint is wrong (R4 / verify-before-claim).** The S208 list says "implement §59 … green-field, flip
the Nominal banner." But:
- `docs/known-gaps.md` (S171 line): **"value-native map §59 BUILT S169 (Nominal→Implemented)."**
- 9 live suites — `value-native-map-{type-system,literal-parser,runtime,ordered-build}-s169` +
  `value-native-map-{codegen-collector,codegen-chunk,codegen-emit,iteration-order-lint}-d4` + the e2e —
  pass **202 / 202** (0 fail, 458 expects). Impl spans type-system.ts, expression-parser.ts, runtime-template.js,
  codegen/{emit-expr,emit-functions,emit-each,emit-logic,emit-client,emit-event-wiring}.ts, lint-w-map-iteration-order.js.
- RATIFIED-DESIGN `docs/changes/map-type-2026-06-06/RATIFIED-DESIGN.md` locks M1-M9; the only decoupled
  remainder (build-decomp item 7, "self-host migration of 130 `new Map`/`new Set` sites") is explicitly
  "NOT a v1 adopter-ship blocker."
- So §59 v1 is done. The S208 list + SPEC-INDEX both carried the dead "Nominal/not-implemented" banner.
- Residual is **doc reconcile only** (flip SPEC §59 Nominal banner → Implemented; reconcile §0 Nominal counts;
  fix this list item) — PA/SPEC territory, not a build. No code change appropriate from the sПА. PARK.

## Close
Whole-list stall confirmed → stand down. Branch `spa/ss13` carries only this disposition bookkeeping (no code
to re-integrate). One re-integration/findings note dropped to `handOffs/incoming/`. The user closes the instance.
