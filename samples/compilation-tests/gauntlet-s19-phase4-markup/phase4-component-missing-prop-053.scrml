// Component invocation missing required prop → E-COMPONENT-010.
<program>
    ${
        const Badge = <span props={
            color: string,
        }>
            <span>${color}</>
        </>
    }
    <Badge/>
</program>
