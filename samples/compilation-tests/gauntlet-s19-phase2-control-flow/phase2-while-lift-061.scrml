// while + lift produces array (§49.6)
${
    let i = 0
    ${
        while (i < 3) {
            lift i * 10
            i = i + 1
        }
    }
    let result = ~
    log(result.length)
    function log(n: number) { let _ = n }
}
<program>
    <p>ok</>
</>
