// `is` operator on enum variant, bare if condition
${
    type Status:enum = { Active, Banned, Pending }
    let s:Status = .Active
    if (s is .Active) {
        let _ok = 1
    }
}
<program>
    <p>check</>
</>
