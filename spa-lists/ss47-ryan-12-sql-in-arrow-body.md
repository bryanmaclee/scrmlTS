# ss47 — `?{}` SQL inside an arrow-function body → E-CODEGEN-INVALID-JS (Ryan #12) — VERIFY-FIRST · MED-HIGH

**Currency:** scoped S224 (PA) @ HEAD `9ad78593` / 2026-06-27. **FIREABLE.** Source = **GitHub issue #12** (rjantz3, v0.7.0). **MED-HIGH** — the compiler emits JavaScript it cannot itself parse on a reasonable pattern (a `?{}` SQL in a local arrow helper). **Same family as the S215 Ryan-PR#1 F1/F3** (server-call / `await` injected into a SYNC arrow callback) — cross-reference that arc.

**Authority (READ FIRST, Rule 4):** `gh issue view 12 --json title,body` (repro) + SPEC §8 (`?{}` SQL) + §19.9.3 (body-split / CPS — `?{}`/server-call lowering) + §12 (route inference / server escalation) + the S215 findings (`handOffs/delta-log.md` [14][15] · repros `/tmp/r26-ryan/finding1-v3.scrml` + `finding3-cps.scrml`) + memory `feedback_adversarial_verify_not_confirmatory`. The bug: a `?{}` SQL block inside an arrow body — `const ins = (x) => { ?{INSERT ...}.run() }` — makes codegen emit unparseable JS (`E-CODEGEN-INVALID-JS`, "the compiler emitted JavaScript it cannot itself parse"; the emitted server fragment mangles the arrow body around the `?{}` lowering). The `?{}` escalates the enclosing fn to server + the CPS/SQL lowering doesn't compose with the arrow-closure shape.

**Fix direction (survey-first — this is the load-bearing decision):** the `?{}`-in-arrow case must either (a) **lower correctly** (the arrow body's `?{}` threads through the server/CPS lowering like a `function` body does — likely the right answer if the pattern is valid scrml), OR (b) **be rejected with a precise actionable diagnostic** (NOT "please report a compiler bug" — a clear "`?{}` is not supported inside an arrow-function body; use a named `function`" steer, the S196 wrong-altitude-diagnostic discipline). Survey decides (a) vs (b) by what the §8/§19.9.3/§12 model actually permits — is a `?{}` in a non-`function` arrow closure a legal server-escalation locus, or is the arrow the wrong vehicle (like the S215 F1 sync-callback `await`)? **PARK the (a)-vs-(b) call to the PA→user if the survey finds it's a genuine design question** (don't let the sPA rule a language-semantics fork). If it's clearly a codegen bug (valid pattern, bad emit), fix it (a); if the arrow is structurally the wrong vehicle, diagnose it (b).

**Parallel-safety:** codegen (the `?{}`/CPS lowering × arrow-closure — `emit-server.ts` / the body-split/CPS path / `emit-logic.ts`). Check vs ss44 (#15, also codegen but session-projection sub-area) at landing — likely disjoint. Disjoint from ss45 (route-inference), ss46 (type-system).

**coreFiles (survey-first):** grep the `?{}` SQL lowering + the body-split/CPS path (`emit-server.ts` / `emit-logic.ts` / the CPS batch planner `cps-batch-planner.ts`) for arrow-function-expression handling; the E-CODEGEN-INVALID-JS validate-emit site (`validate-emit.ts`) that catches it. SPEC §8 / §19.9.3 / §12.

**Brief reminders:** **VERIFY-FIRST (S138 reverse)** — reproduce #12 on HEAD `9ad78593` (compile the issue's `(x) => { ?{INSERT ...}.run() }` repro → confirm E-CODEGEN-INVALID-JS + inspect the mangled emit) BEFORE fixing. **This is the S215-class shape — ADVERSARIAL is MANDATORY** (a non-PA-pipeline-reported codegen bug): enumerate the blast radius (`?{}` in: a `.map`/`.filter` sync callback, a CPS continuation, a SQL-init, an `!{}` arm, a nested arrow) and construct repros for each; run `/code-review` high/ultra OR a finder fan-out on the diff. R26 (compile + `node --check` the emitted server fragment) + the full suite. If the fix is (b)-diagnose, NAME the code (e.g. `E-SQL-IN-ARROW-BODY` or reuse a §8 code) and land the §34 row with the impl. **Surface the (a)-vs-(b) survey verdict to the PA before landing if it's a design fork.**

## Items
1. **Verify + survey (a)-lower vs (b)-reject** `[status=open]` VERIFY-FIRST — reproduce on HEAD; inspect the mangled emit; determine whether `?{}`-in-arrow is a valid server-escalation locus (→ fix the lowering) or structurally wrong (→ precise diagnostic). PARK to PA→user if a genuine language-semantics fork.
2. **Fix (lower correctly OR diagnose cleanly)** `[status=open]` — per the survey verdict: thread the `?{}`/CPS lowering through the arrow body, OR emit a precise actionable diagnostic (NOT "report a compiler bug"). NAME + catalog any new §34 code with the impl.
3. **Adversarial + tests** `[status=open]` — the S215 blast-radius repros (`?{}` in map/filter/CPS/SQL-init/`!{}`-arm/nested-arrow) + `/code-review` high/ultra; regression test (REAL source, `node --check` the emit).

## Acceptance
The #12 repro (`?{}` in an arrow body) either compiles to valid `node --check`-clean JS, OR fails with a precise actionable diagnostic naming the arrow-body constraint (no "report a compiler bug" message); the adversarial blast-radius shapes all behave consistently (lowered or diagnosed, never invalid-JS); full suite green; if a design fork, the PA-ruled disposition is recorded.
