# progress — bug-61-compound-rollup-read-path-2026-05-28

(append-only, timestamped. Agent updates after each step. WIP commits expected.)

- DISPATCH (S140): brief at BRIEF.md. Read-path sibling of Bug 58 (RESOLVED 29c33a6c). Agent has not yet started.

## S140 agent (re-dispatch v2)
- 2026-05-29 START: pwd=/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-aaa1bc622077a5cfe. Merged main (ff e1630e93..f8aac7db). 57/58/59 markers present (rewriteExprArrowBody=5, each-block case=1, _flatBindKey=3). bun install + pretest OK.
- Mapped threading surface: collector reactive-deps.ts (mirror collectDerivedVarNames + emit-synth-surface key-gen). Context field: EmitExprContext (emit-expr.ts:48), EmitLogicOpts (emit-logic.ts:90), CompileContext (context.ts:40), IfOpts (emit-control-flow.ts:104), EmitValidatorOpts (emit-validators.ts:95). Populate: index.ts:631+703 (collectSynthCellKeys). Thread: emit-reactive-wiring.ts:297-299 emitOpts (both ternary branches), _makeExprCtx (emit-logic.ts:486), emit-logic.ts:1602 (validators), emit-logic.ts:2189 (emitIfStmt), emit-control-flow.ts:224+234, emit-validators.ts:165, emit-event-wiring.ts:976/988/1055 (ctx.synthCellKeys), emit-variant-guard.ts:346+402. Guard+synthDottedKey reusable from 5ec3319e scaffold (add ctx.synthCellKeys?.has(dotted)).
- 2026-05-29 STEP 1: collector collectSynthCellKeys (reactive-deps.ts) + EmitExprContext.synthCellKeys + EmitLogicOpts.synthCellKeys + CompileContext.synthCellKeys + IfOpts.synthCellKeys + EmitValidatorOpts.synthCellKeys. Populate index.ts:631+703 + makeCompileContext default. Thread: _makeExprCtx (emit-logic), emit-reactive-wiring emitOpts (BOTH ternary branches via synthCellKeysSpread, ungated on derivedNames.size), validator sidecar, emitIfStmt, emit-control-flow _ifExprCtx+bodyOpts, emit-validators exprCtx, emit-event-wiring ×3 (varRefs/server-fn paths) + bool-attr condExpr (line 859 = the disabled=!@form.isValid bug site) + conditional-display condExpr, emit-variant-guard. Guard+synthDottedKey in emitMember (ctx.synthCellKeys?.has(dotted) membership = over-fire guard). Conf test conf-compound-rollup-read-bug-61.test.js: compound isValid→dotted, reactive submitted→dotted, per-field name.isValid→dotted, OVER-FIRE config.errors/submitted/isValid→member, real-field form.name→member. FAIL-BEFORE verified (stash pop/push: 3 fail pre-fix — isValid/submitted/per-field member access; over-fire+real-field pass both). 13/13 pass post-fix. FULL pre-commit suite 15101 pass / 0 fail / 88 skip. Canonical interp is ${@x} NOT ${@x/} (the crashed-branch test used the malformed /} form → acorn parse fail → string fallback).
- 2026-05-29 STEP 2: happy-dom acceptance gate + formFor structured-exprNode fix. KEY FINDING: the §41.14 SYNTHESIZED formFor submit button (emit-form-for.ts:buildSubmitButton -> exprAttr disabled=!@<cell>.isValid) builds a STRING-only expr attr (no exprNode) -> falls through emit-event-wiring's bool-attr handler to rewriteExprWithDerived (STRING rewrite path) which does NOT collapse synth keys. Hand-authored disabled=!@form.isValid gets a parsed exprNode (structured path -> emitMember guard fires). FIX: exprAttr now parses raw via parseExprToNode (mirrors logicInterpolationNode) so the synth gate routes through the canonical emit-expr path. Now emits _scrml_reactive_get("signup.isValid"). collectSynthCellKeys DOES find the synth compound (_cellKind compound-parent, in walked nodes post-Bug-58). Strengthened Bug-58 browser test: was documented-but-unasserted disabled-gate (only asserted initially-disabled, NOT enable-on-valid, "blocked by read-path bug"). Now asserts the enable-on-valid flip: fill valid values -> signup.isValid true -> button disabled attr REMOVED. happy-dom 10/10 pass. Full pre-commit suite 15101 pass / 0 fail. Browser suite 248 pass / 0 fail.
- 2026-05-29 STEP 3 (R26): recompiled 5 real-source cases (no synthetic AST).
  (1) canonical formFor: 0 errs, node --check OK, signup.isValid -> dotted, NOT member. Submit button verified runtime-enableable in happy-dom (Step 2).
  (2) hand-authored compound: 0 errs, OK, form.isValid + form.submitted -> dotted; form.name display slot -> member (non-regression).
  (3) plain-cell <config>={errors,submitted,isValid}: 0 errs, OK, config.errors -> MEMBER (correct), NOT dotted (over-fire guard holds).
  (4) gauntlet-s79-signup-form (real adopter): bare @isValid -> _scrml_reactive_get("isValid") (NOT over-fired to a dotted key); no isValid.* dotted. NB pre-existing E-TYPE-025 (match @formState on asIs subject, line 162) unrelated to Bug 61 — sample untouched by my work (git diff 0e986b71 HEAD on sample = empty); the node --check fail there is downstream of that pre-existing compile error producing incomplete output, NOT a Bug-61 regression.
  (5) per-field 3-seg @form.name.isValid: 0 errs, OK, -> dotted, NOT member.
  pretest: all 13 samples compile. R26 PASS. Bug 61 fix complete end-to-end.
