// :> arrow — NOT in §18.2 grammar. Only => and -> are valid.
${
    type Dir:enum = { North, South }
    let d:Dir = .North
    let s = match d {
        .North :> "up"
        .South :> "down"
    }
    log(s)
    function log(s: string) { let _ = s }
}
<program>
    <p>ok</>
</>
