# Progress: f-compile-001

Append-only log. Timestamps in local time.

- [start] Worktree branch `changes/f-compile-001` created off `3dab098` (S50 close).
- [start] Worktree path verified: `/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-aa02e8b34085db2b4`
- [start] git rev-parse confirmed worktree toplevel matches; tree clean.
- [setup] `bun install` in worktree compiler/ (no node_modules yet) — succeeded.
- [setup] `bash scripts/compile-test-samples.sh` ran cleanly (12 samples → dist/).
- [baseline] `bun test` from worktree root: **8196 pass / 40 skip / 0 fail / 8236 tests / 385 files**. Matches main checkout exactly.
- [repro] `bun run compiler/src/cli.js compile examples/23-trucking-dispatch/`: produced 17 HTML / 28 client.js / 17 server.js from 32 sources. Collision confirmed.
- [discovery] **E-CG-002 conflict.** SPEC.md line 12297 reserves E-CG-002 for "conflicting output paths" but `emit-server.ts:76` uses it for "boundary function has no generated route name". Pre-existing SPEC/impl drift. Will use **E-CG-015** instead (next-available after E-CG-014). SPEC.md line 12297 will be updated to reflect actual emit-server.ts usage, and a new entry for E-CG-015 will be added. Surfaced as scope-adjacent, not scope-expansion.
- [plan] Decomposition: (1) write pre-snapshot.md + commit; (2) implement Option A in api.js (path-relative write loop); (3) implement Option B (collision detection + E-CG-015) in api.js; (4) add integration tests; (5) update SPEC.md §47 with normative output-path subsection + E-CG-015 entry; (6) update FRICTION.md F-COMPILE-001 RESOLVED; (7) verify dispatch app E2E; (8) re-run full test suite.
- [survey] Surveyed `compile.js`, `build.js`, `dev.js`, `serve.js`. Findings:
  - **`api.js`** lines 499-558 (write loop using `basename(filePath, ".scrml")`) is THE collision site. Central fix here.
  - **`compile.js`** does NOT need touching. It defers all path/output handling to `compileScrml()` in api.js.
  - **`serve.js`** does NOT need touching. Compile-on-demand only; no dist scanning.
  - **`build.js` line 122 `discoverServerRoutes(outputDir)`** uses flat `readdirSync(outputDir)` — must walk the tree recursively after Option A so nested `*.server.js` files are found. ~5 line change.
  - **`dev.js` line 140 `loadServerRoutes(outputDir)`** uses flat `readdirSync(outputDir)` — same flaw as build.js. ~5 line change.
- [coordination decision] **dev.js `loadServerRoutes` IS in scope for W0a**, but the change is purely path-walking (orthogonal to W0b's STDLIB-resolution / `serve-client.js` / `runtime-template.js` / `module-resolver.js` territory). Without this fix dev mode is broken on nested apps after Option A — not acceptable to ship Option A without the dev.js update. Will share the recursive-walk helper between `build.js` and `dev.js` to keep them in sync. **If W0b also touches `loadServerRoutes` for any reason, supervisor merge order must put W0a first; otherwise the W0b agent should rebase.** Surfacing for visibility, proceeding with the change.
- [coordination decision] Will introduce a small shared helper `findServerJsFilesRecursive(dir)` and put it in api.js (or a new tiny utility module) to be imported by both build.js and dev.js. This minimizes drift between the two and avoids duplicated traversal logic.
- [impl-1] api.js: added `computeOutputBaseDir(inputFiles)` (segment-aligned common-prefix helper) and `findOutputFiles(dir, suffix)` (recursive walker). Replaced flat write loop with tree-preserving loop. Added Option B collision tracking via `writtenPaths` Map; E-CG-015 emitted on duplicate dist path. Committed `05dc7fb`.
- [impl-2] build.js: `discoverServerRoutes` now uses `findOutputFiles`; `filename` field carries relative path so `generateServerEntry` import statement works for nested paths. Committed `99d4909`.
- [impl-2] dev.js: `loadServerRoutes` now uses `findOutputFiles`; absPath used for file:// import URLs (cache-buster preserved); relPath used for diagnostics. Committed `99d4909`.
- [verify-e2e] `bun run compiler/src/cli.js compile examples/23-trucking-dispatch/`: **21 HTML / 32 client.js / 21 server.js** in nested dist/ tree (was 17/28/17 flat). 7 dist subdirs (pages/{auth,customer,dispatch,driver}, components, models, root). All 32 source files now have unique outputs. Zero collisions. **F-COMPILE-001 fixed end-to-end.**
- [verify-tests] Full test suite: **8196 pass / 40 skip / 0 fail / 8236 tests** — IDENTICAL to baseline. Zero regressions. samples/compilation-tests/ uses single-file CLI invocations, so the common-base reduction to dirname keeps output flat (unchanged behavior).
- [verify-build] `bun run compiler/src/cli.js build examples/23-trucking-dispatch/`: discovered 36 routes + 12 WS channels across nested *.server.js files. _server.js generated with correct `import ... from "./pages/customer/home.server.js"` style relative paths.
- [observation] **Pre-existing duplicate-import bug in `_server.js`.** `generateServerEntry` emits one `import { _scrml_session_destroy }` per server.js module — `_scrml_session_destroy` is exported by every server.js with auth/session middleware, so the import statement collides on subsequent declarations:
  ```
  SyntaxError: Identifier '_scrml_session_destroy' has already been declared
  ```
  Confirmed reproducible on main checkout (pre-W0a) — this bug pre-exists this fix; F-COMPILE-001's resolution makes it MORE visible because there are now 21 server.js files instead of 17, but the underlying collision was already there. **Out of scope for W0a.** Recommend a separate finding (call it F-BUILD-002 or similar) tracked in FRICTION.md / hand-off so the supervisor can dispatch a separate fix. Probably just needs `import * as _m1` aliasing or de-duped exports. Reporting in final summary.
- [tests] Wrote `compiler/tests/integration/compile-output-tree.test.js`: 17 tests across 7 sections (§1–§7). Initial run revealed expected-vs-actual mismatch on §1 (deepest-common-prefix algorithm picks `/pages/` as common base when both files are under `pages/`, so output drops the `pages/` prefix). Adjusted expectations to match the algorithm and added §1c that mimics the dispatch-app shape with a top-level anchor file. All 17 pass.
- [tests] Full test suite re-run with new file: **8213 pass / 40 skip / 0 fail / 8253 tests / 386 files**. Delta: +17 pass, +1 file (matches new test count exactly). Zero regressions.
- [spec] SPEC.md updated: §47.9 (Output Path Encoding) added with 7 subsections (rule for outputBase, per-artifact path, collision contract, normative statements, three worked examples). E-CG-002 row in the §34 errors table corrected to reflect actual emit-server.ts:76 usage; E-CG-015 added to both §34 summary and §47.7 errors tables.
- [docs] FRICTION.md F-COMPILE-001 marked RESOLVED with banner pointing to fix commits + SPEC §47.9. M6 reconfirmation gets a footer linking back. README updated: Run section replaced "F-COMPILE-001 P0 / 17 HTML observed" paragraph with post-W0a reality (21/32/21 in nested tree). Friction-surface bullet annotated "RESOLVED 2026-04-30 W0a".
- [verify] Final dispatch-app E2E: 32 sources → 21 HTML / 32 client.js / 21 server.js / 8 dist subdirs. Each of the 32 sources now has its own unique output. Zero collisions. Full test suite still 8213 pass / 0 fail.
- [out-of-scope find] **Pre-existing `_server.js` duplicate-import bug.** `generateServerEntry` in build.js emits `import { _scrml_session_destroy } from "./X.server.js";` once per server.js module — but `_scrml_session_destroy` is exported by every server.js with auth middleware, so subsequent imports collide and Node/Bun fails with `SyntaxError: Identifier '_scrml_session_destroy' has already been declared`. Confirmed reproducible on main checkout (pre-W0a) — pre-existing bug unrelated to F-COMPILE-001. F-COMPILE-001 makes it MORE visible (21 instead of 17 colliding imports) but doesn't cause it. **Recommend: open a new finding (F-BUILD-002 or similar) for the supervisor to dispatch as a separate fix.** Probably needs `import * as _m1` aliasing per module, OR de-dup of common runtime exports.
- [out-of-scope find] **Build.js `_server.js` issue compounds even more** because the W-DEPLOY-001 message says "36 server functions found" — that's based on counting routes across all server.js files, but each file's `_scrml_session_destroy` is the SAME runtime export pattern. The duplicate-import bug means even a successful `scrml build` produces an unrunnable `_server.js`. Same pre-existing nature.
- [final commits] 6 incremental commits + this final progress update. Ready for supervisor review.
