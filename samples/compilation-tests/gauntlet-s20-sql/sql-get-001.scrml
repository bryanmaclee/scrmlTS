// Phase 6: ?{}.get() — single row SELECT (§8.3)
// Should compile clean.

< db src="./test.db" tables="users">
${
  let user = ?{`SELECT id, name FROM users WHERE id = 1`}.get()
}
<p>User loaded</>
</>
