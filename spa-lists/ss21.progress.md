# sPA ss21 — progress log (render + expression codegen)

**Branch:** `spa/ss21` (off main/origin-main `cf9f1109`, contains ss20 squash `8a0e9e3d`) · **Worktree:** `../scrml-spa-ss21`

## Boot (2026-06-25 ~1410)
- Branched `spa/ss21` from `cf9f1109` (== origin/main; local main since advanced to `9ac06830` = s221 boot bookkeeping, touches NONE of my target files).
- dist + node_modules symlinked (S209).
- **Briefs now include a `git merge origin/main` base-currency startup step** (the S112 fix I missed on ss20).
- Footprint corrections: item 5 runtime locus is `runtime-template.js` (dist/ is the artifact); the `<errors>` (item 5) and `if-chain-branch` (item 3) BOTH live in `emit-event-wiring.ts` (kind enum L103) → item 5 WAVED after item 3.

## Wave plan (collision-aware)
- **Wave 1 (parallel, disjoint files):** items 1+2 (emit-lift), item 3 (emit-html+event-wiring if-chain), item 4 (emit-expr).
- **Wave 2 (after item 3 lands):** item 5 (errors-element in emit-event-wiring + runtime) — avoids the emit-event-wiring.ts collision with item 3; also "heaviest, ordered last".

## Dispatch — Wave 1 (scrml-js-codegen-engineer · worktree · opus)
| Item | change-id | agentId | status |
|---|---|---|---|
| 1+2 emit-lift request-bare-if + tableFor slot (LOW+MED) | ss21-lift-request-tablefor-interp | a7a52bbf12519eeb7 | dispatched |
| 3 g-if-chain-branch-display-null-interp (MED) | ss21-ifchain-display-gate | a436243badf723eba | dispatched |
| 4 g-unary-left-of-exponent-no-paren (MED) | ss21-exponent-unary-paren | a15445ee85b14b2ed | dispatched |
| 5 g-errors-anchor-not-reactively-clearing (MED) | ss21-errors-reactive-clear | — | WAVE 2 (after item 3) |

Landing = cherry-pick onto spa/ss21 (S112 base-currency briefed; agents branch from origin/main == base). Items 4 + item-2 test-strengthen carry the S215 adversarial mandate.

## Events
- **Item 1 SALVAGED + landing.** Combined items-1+2 agent a7a52bbf disconnected mid-response (API connection-closed; classifier unavailable) AFTER committing item 1 (`adb6eb2b`), clean tree, item 2 NOT started. Item-1 fix is in **emit-reactive-wiring.ts** (LOCUS REFINEMENT vs the emit-lift.js footprint — the reactive-dep detector missed `_scrml_request_<id>` member reads, dropping the bare-if lift into the non-reactive branch). Cherry-picked onto spa/ss21; **item-1 test 4/4 GREEN** (self-verified — agent disconnected + classifier down). Commit running bg (bjkm3wqpb).
- **Item 2 RE-DISPATCHED** standalone (agent a202e3d4eabc12b08, change-id ss21-tablefor-column-slot-interp) — independent of item 1 (emit-lift.js vs emit-reactive-wiring.ts). Transient-disconnect handled per S211 protocol (re-check agent HEAD; salvage committed, re-dispatch unstarted).
- **Item 1 LANDED `808bc135`** (bg commit). NOTE: spa/ss21 unexpectedly carries the PA's local `9ac06830` bookkeeping commit as base+1 (harmless — PA's own sPA-list-rebuild, touches none of my 5 files; PA re-integration unaffected since 9ac06830 is already on local main).
- **Item 3 STALLED→SALVAGED.** Agent a436243badf723eba hit the 600s stream watchdog AFTER committing `d93ae5eb` (clean tree, 1-ahead, ss20 present) — it stalled on a redundant POST-commit full-suite run. Self-verified: item-3 browser test **13/0 GREEN** in its worktree. Files: binding-registry +12, emit-event-wiring +139/-44, emit-html +31, browser test +301. Pending cherry-pick (after item-4 commit frees index).
- **Item 4 LANDING (bg b9gxgzizi)** `bd74d63c` — PARTIAL: Bug A (parenthesized `(-@a)**2`, the genuine binaryOperandNeedsParens case) FIXED + twin emitStringFromTree; full S215 matrix green, 23 tests, suite 25246/0. **Bug B PARKED → PA** (the list's literal `-@a**2` = acorn parse-error → silent regex-fallback invalid JS; needs a parser-level accept-or-reject decision).

## ⚠️ WEEKLY USAGE LIMIT HIT (resets 7am America/Denver)
The item-2 re-dispatch agent (a202e3d4) reported "You've hit your weekly limit" — model dispatch is now constrained. sPA continues landing/verifying (local hooks unaffected by the model limit); NO further agent re-dispatches. Item 5 (a9689fe31eb466fe6) was dispatched before the limit — may complete or be cut off; salvage if cut off.

## Item 2 SALVAGED + landing (bp1su356x)
Agent a202e3d4 did the fix + browser test but was cut off by the weekly limit BEFORE committing (uncommitted STAGED work) and before the r28 strengthen. sPA:
- Backed up + salvaged the 2 files onto spa/ss21 (base parity verified — clean file-delta).
- **LOCUS REFINEMENT:** fix is in `emit-table-for.ts` (dedicated tableFor emitter), not emit-lift.js. Adds splitTableForTextInterp + rewriteAtDotInNodeExpand (ss17 each-body split prior art).
- Did the r28-bug-2 test-strengthen **sPA-direct** (the agent was cut off before it): added `not.toContain("${@row.name}")` to the lone masking `toContain("row.name")`.
- Verified on spa/ss21: r28-bug-2 6/0, new browser test 12/0. Commit running bp1su356x.

## Item 5 LANDING (biwh7vv3l) — completed before any cutoff
Agent a9689fe31eb466fe6 chose **Option B** (errors emitter): wrap `render_<suffix>()` in `_scrml_effect` (auto-tracks the derived read) instead of `_scrml_reactive_subscribe` on the derived `.errors` key. Rejected Option A (runtime-template.js derived-fires-subscribers — load-bearing, broad blast radius). Edited ONLY the errors-element region of emit-event-wiring.ts → clean auto-merge with item-3's if-chain-branch gate (different region). runtime-template.js NOT touched. RED 4/0→GREEN 4/0; suite 25227/0. Verified integrated: c11 35/0, errors browser 4/0.

## CLOSED 2026-06-25 1902 — ALL 5 items landed, branch tip `760254a9`
- Item 1 `808bc135` (locus→emit-reactive-wiring) · Item 4 `b85d9795` (Bug A; Bug B parked) · Item 3 `6f06044d` · Item 2 `0d78b7ec` (locus→emit-table-for, salvaged + sPA-direct r28 strengthen) · Item 5 `760254a9` (Option B).
- spa/ss21 = base `9ac06830` + 5 fixes, clean linear, `0 6` vs origin/main → clean PA merge.
- Integrated browser sweep: **275/0 across 36 files**; ss20 if-guard intact; c11 errors 35/0.
- Re-integration message: `handOffs/incoming/2026-06-25-1902-from-spa-ss21-reintegration.md`.
- Bug B VERIFIED loud on a real build (E-CODEGEN-INVALID-JS, exit 1) — not a silent gap, but a poor self-blaming diagnostic.
- Infra: weekly limit hit + 3 agent failures (disconnect/stall/cutoff) — ALL recovered (salvage + self-verify); r28 strengthen done sPA-direct. S112 base-currency + S220 scratchpad-race discipline applied. sPA STOOD DOWN.

## Landed (final — 5 of 5)
- Item 1 `808bc135` · Item 4 `b85d9795` (Bug A) · Item 3 `6f06044d`. spa/ss21 tip after item 3 = `6f06044d`, base = `9ac06830` (PA bookkeeping atop cf9f1109). Tree clean; all fix markers + ss20 intact.
- Item 5 (errors) dispatched (a9689fe31eb466fe6, Wave 2) — brief flags the emit-event-wiring.ts region overlap with item 3 for clean reconciliation. Item 2 (tableFor) re-dispatch still running (a202e3d4).

## NEW deferred findings (item 4 — surface to PA)
- **Bug B** — `-@a ** 2` UNparenthesized: parse-error → `rewriteReactiveRefs` regex fallback silently emits invalid `- _scrml_reactive_get("a") ** 2`. PARSER-LEVEL language decision (accept as `(-@a)**2` or reject with a diagnostic). The original list item-4 "LOUD E-CODEGEN" framing is WRONG for this repro — it's SILENT.
- **validate-emit — VERIFIED, NOT a shipping gap.** sPA compiled `-@a ** 2` via the real CLI (`bun run compiler/src/cli.js compile`): it FAILS LOUDLY — `error [E-CODEGEN-INVALID-JS]`, `FAILED — 1 error`, exit 1. So the original list "LOUD" framing for Bug B is CORRECT on a real build; the agent's `write:false` API probe not flagging it is a write:false-path artifact, NOT a shipping risk. **BUT diagnostic-quality issue (feedback_dont_soft_classify_bugs):** the error self-blames ("the compiler emitted JavaScript it cannot itself parse") instead of naming the root cause ("`-@a ** 2`: unary left operand of ** needs parens"). The parser-level Bug-B fix should ALSO improve this diagnostic.
- **double-unary-minus emitUnary bug** — `Unary(-, Unary(-, a))` serializes as `--a` (decrement token) in BOTH emit-expr emitUnary + emitStringFromTree; invalid JS for numeric (`--3`). Separate pre-existing emitUnary bug.
