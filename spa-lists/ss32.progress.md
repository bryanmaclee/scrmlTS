# ss32 — auto-await reactive-server hardening · progress

- **Item 1 — g-auto-await-reactive-server-no-error-arm (MED)** → `landed-on-branch` SHA `d39f9d93` on `spa/ss32`.
  - Dispatched scrml-js-codegen-engineer (worktree `agent-a11c7bfb`, opus). Fix: uniform `.catch(... => _scrml_error_boundary_log(name, err))` on the auto-await IIFE call (`emit-client.ts` post-server-fn-iife-wrap).
  - sPA verify: re-ran target test independently (16/0); cherry-pick onto spa/ss32 re-ran the full pre-commit gate (green) + post-commit gauntlet (Browser: all checks passed). R26 before/after: `unhandledRejection` fired → no longer fires, surfaces a scrml error.
  - Agent justified deviating from the briefed `_scrml_error_boundary_uncaught` (it returns a throwable → would re-create the unhandledrejection); chose the loud, non-swallowing `_scrml_error_boundary_log` (always-included `errors` chunk). Accepted.
  - **Discovered fork (g):** `@cell = serverFn() !{...}` emits a DEAD `!{}` dispatch (wrap captures the Promise). Pre-existing, not a regression. Escalated to PA. Same root as item 2.

- **Item 2 — g-auto-await-read-before-resolve-race (LOW, survey-first)** → `parked` (REPRODUCES, design-ruling required → PA).
  - Survey (continuation of the item-1 agent). VERDICT: **REPRODUCES narrowly** — top-level `@derived = @data.field` off an async-server cell is a bare one-shot `_scrml_reactive_set` (no `_scrml_effect` subscription) → reads pre-resolve. No-init → `TypeError` breaks page init; with-init → stale forever. Display bindings `${@data.value}` are race-free (subscribe via `_scrml_effect`).
  - **Parked, not dispatched:** fix is a designer-axis call (dependency-barrier vs language-wide reactive-derivation vs hybrid vs lint-diagnostic). sPA does not make design rulings. Survey doc `docs/changes/g-auto-await-read-before-resolve-race/SURVEY.md`.
  - Shares structural root with item-1 fork (g): "sync successor reads async-deferred producer pre-resolve." A single barrier could subsume both.

**End-state:** item 1 = landed-on-branch (`d39f9d93`); item 2 = parked (design ruling → PA). 0 dropped.
**Owed:** flogence reply re item 1 (per list brief) — PA-owned at re-integration.
