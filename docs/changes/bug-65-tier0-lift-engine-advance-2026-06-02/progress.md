# Bug 65 — Tier-0 `${for…lift}` engine `.advance(...)` miscompile — progress

Change-id: bug-65-tier0-lift-engine-advance-2026-06-02
Worktree: /home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-af38ebab7b2bd4502
Branch base: 57edc794 (source HEAD); maps materialized from 358581a8 (docs-only)

## Log (timestamped, append-only)

2026-06-03T04:21:33Z — Startup verified: pwd OK (worktree-prefixed), git toplevel OK, HEAD 57edc794, maps materialized from 358581a8 (docs-only, no source delta), bun install OK, bun run pretest OK (13 samples).
2026-06-03T04:41:03Z — FIX IMPLEMENTED + verified. Shared the Bug 62 ctx helper (exported buildEachEngineCtx + emitEngineHandlerBody + EachEngineCtx from emit-each.ts; emit-lift re-packs via buildLiftEngineCtxFromExtras adapter — no fileAST re-walk, no duplicated lowering). Threading chain: emit-logic.ts:2385 (for-stmt dispatch now threads engine extras, mirroring if-stmt) → emit-control-flow emitForStmt builds carrier + threads into emitConsolidatedLift/emitLiftExpr/emitIfStmtWithContainer/emitForStmtWithContainer → emit-lift emitCreateElementFromMarkup + emitSetAttrs handler sites route engine refs through emitEngineHandlerBody. Empirical (post-fix): state-plane repro → _scrml_engine_advance("phase","Active",...) [1], zero raw .advance, node --check exit 0. msg-plane → _scrml_engine_dispatch_message("dragPhase",{variant:"Drop",data:{col:col}},...). assign → _scrml_engine_direct_set("phase","Active",...). tree-shake (onclick=ping(it)) → ZERO engine helpers. NOTE: onclick=ping(@.id) in Tier-0 for-lift is a SEPARATE pre-existing @.-lowering bug (E-CODEGEN-INVALID-JS on ORIGINAL code too — NOT my regression); §5 tree-shake test uses ping(it) to avoid it. Unit test 12/12 pass.
2026-06-03T04:45:46Z — Browser canary added (lift-engine-advance-bug65.browser.test.js, 6/6 pass): §0 runtime defines _scrml_engine_advance, §1 state click Idle→Active, §2 message click runs (Dragging×Drop) arm + transition with col payload, §3 assign click direct-set. This is the LOAD-BEARING canary — drives real clicks on for-lift <li>, asserts engine variant advances (emit-string-only test would miss Bug 65 since it is node --check-clean). Bug 62 browser regression guard: 5/5 still pass. Unit+integration+conformance subset post-fix: 15675 pass / 89 skip / 1 todo / 0 fail (baseline 15663 pass; +12 from new unit file, zero new failures).
2026-06-03T04:52:17Z — DONE. Full suite (bun run test) post-fix: 22771 pass / 220 skip / 1 todo / 0 fail (baseline 22753 → +18 from new Bug 65 tests, zero new failures). Phase-3 empirical re-verified on committed build (64d64aa9): state→_scrml_engine_advance, msg→_scrml_engine_dispatch_message, assign→_scrml_engine_direct_set, tree-shake→zero engine helpers, all node --check exit 0. git status clean; branch tip==HEAD; divergence 0/4 from base; no source leak into main (only PA's BRIEF.md archival untracked there). Commits: 3c7ee0e8 (startup+maps) / 26bd4578 (fix+unit) / 93292701 (browser canary) / 64d64aa9 (known-gaps RESOLVED).
