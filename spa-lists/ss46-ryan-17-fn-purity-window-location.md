# ss46 — fn-purity accepts `window.location` reads (Ryan #17) — VERIFY-FIRST · MED

**Currency:** scoped S224 (PA) @ HEAD `9ad78593` / 2026-06-27. **FIREABLE** (but SEQUENCE vs other type-system.ts lanes — see parallel-safety). Source = **GitHub issue #17** (rjantz3, v0.7.0). **MED** — a soundness hole in the purity contract: `fn` is the determinism guarantee (§48.3/§48.11) and the rest of the compiler may rely on it; this lets a non-deterministic body be `fn`, and `I-FN-PROMOTABLE` actively *steers* adopters into mislabeling.

**Authority (READ FIRST, Rule 4):** `gh issue view 17 --json title,body` (repro) + SPEC §48.3.3 (the `fn` body constraints / non-determinism prohibition) + §48.11 (`fn` = pure) + §33 (deprecated `pure`) + the existing E-FN-004 non-determinism check + the I-FN-PROMOTABLE lint. The bug: a `fn` whose body reads `window.location` (external, mutable browser state) compiles with NO `E-FN` error, and `I-FN-PROMOTABLE` recommends promoting such a `function` to `fn`. `window.location.search` is non-deterministic — a zero-arg `fn` returns different values across calls (after navigation), breaking referential transparency. Note: the PRIMER §6 already names `Date.now()`/`Math.random()` as non-determinism that E-FN-004 catches — `window.location` (+ `document.*` / `navigator.*` / other mutable browser globals) is the same class, currently UNcaught.

**Fix direction (survey-first):** extend the non-determinism check (E-FN-004 class) so reads of mutable browser/global state — `window.location` (+ the broader `window.*` / `document.*` / `navigator.*` mutable surface) — disqualify a `fn`/`pure` body, AND gate `I-FN-PROMOTABLE` so it does NOT suggest promoting a `function` whose body reads them. Define the disqualifying set precisely (survey the existing E-FN-004 non-determinism list — `Date.now`/`Math.random`/`crypto.*`? — and extend consistently; the §41.19 `scrml:time.now()` / §41.20 `scrml:random` capability-scoped surfaces are the sanctioned forms, already E-FN-004-gated). Keep genuinely-pure reads (e.g. `URLSearchParams` parsing of a STRING ARGUMENT) allowed — only the GLOBAL-state read disqualifies. Binding-aware (a user's own `function location()` is not gated — mirror the §41.19/§41.20 binding-aware precedent).

**Parallel-safety:** `type-system.ts` (the `fn`-body purity walker / E-FN-004) + the I-FN-PROMOTABLE lint. ⚠️ **`type-system.ts` is HOT** — ss42 just landed there, ss43 (multi-scrutinee match) will touch it. **SEQUENCE vs ss43** (or intersect carefully at landing per S211). Disjoint from ss44/ss45/ss47.

**coreFiles (survey-first):** `compiler/src/type-system.ts` (the `fn`-body purity/non-determinism walker — grep E-FN-004 + the non-deterministic-global list) + the I-FN-PROMOTABLE lint (`lint-i-fn-promotable*` or wherever it lives — grep `I-FN-PROMOTABLE`). SPEC §48.3.3 / §48.11.

**Brief reminders:** **VERIFY-FIRST (S138 reverse)** — reproduce #17 on HEAD `9ad78593` (compile the issue's `fn getN()` reading `window.location.search` → confirm NO E-FN-004; flip to `function getN()` → confirm I-FN-PROMOTABLE fires) BEFORE fixing. R26 + unit tests. **ADVERSARIAL (S215)** — adjacent: `document.cookie`, `navigator.userAgent`, `window.innerWidth`, a pure `URLSearchParams(argString)` (must STAY allowed), a user-shadowed `location` binding (must NOT fire), `localStorage`. Don't over-fire on genuinely-pure host calls (`Math.max`, the `scrml:math` surface). FULL `bun run test`; re-baseline within-node allowlist if shifted (S198).

## Items
1. **Verify + define the disqualifying global-read set** `[status=open]` VERIFY-FIRST — reproduce; survey the existing E-FN-004 non-determinism list; define the precise mutable-browser-global set (`window.location`/`window.*`/`document.*`/`navigator.*`) to add, binding-aware, keeping pure-arg reads allowed.
2. **Disqualify in the purity check + gate the promote-lint** `[status=open]` — a `fn`/`pure` body reading the set fires E-FN-004 (class); I-FN-PROMOTABLE no longer suggests promoting a `function` reading the set.
3. **Tests** `[status=open]` — regression for both the reject (E-FN-004 on `window.location` in `fn`) and the lint-suppression (no I-FN-PROMOTABLE on such a `function`); plus the must-NOT-fire cases (pure-arg `URLSearchParams`, shadowed binding).

## Acceptance
`fn getN() { ... window.location.search ... }` fires E-FN-004 (was clean); `function getN()` with the same body does NOT get I-FN-PROMOTABLE (was suggested); a genuinely-pure `function` (Ryan's `numOrAbsent`) STILL gets the suggestion; pure-arg `URLSearchParams(s)` + a user-shadowed `location` do NOT fire; full suite green + allowlist rebaselined if shifted.
