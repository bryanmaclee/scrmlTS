# scrml — Session 222 (CLOSE)

**Date:** 2026-06-26. **Profile:** A — FULL. A huge strategic + execution session: the **compiler-reimagining fork RULED — narrow-Road-B COMMITTED + de-risked** (3 design slices + FBIP feasibility), the **mechanics-beauty prereqs resolved** (Set landed B2 · FBIP de-risked · tuples rejected), ss31 codegen landed — plus a hairy **non-isolated-sPA shared-checkout recovery saga**.

> Mechanical stream → `handOffs/delta-log.md` [109]–[126]. Board: `bun scripts/state.ts`. Recent: `bun ../flogence/scripts/digest.ts scrml --fresh`. This carries the IRREDUCIBLE (narrative · open threads · recovered anomalies).

## 🚨 NEXT-START
Boot Profile A (the strategic + language-design threads need the expert context). Board @ close: **HIGH 0 · MED 16 · LOW 13 · v0.7.0 · suite 25474/0/214.** Tree clean — only `main` + `spa/ss36` (the tuple SURVEY historical record; no worktree). Coherence 0/0. NO deputy (S219).

## ⏸️ OPEN — S223 (priority order)
1. **🎯 THE COMPILER-REIMAGINING PROGRAM — narrow-Road-B is COMMITTED** (`docs/changes/compiler-reimagining-derisk-2026-06-26/RULING.md`). De-risk DONE. Next phase = prereqs → the lexer-in-scrml build.
   - **FBIP** (the crux, de-risked FAVORABLY — `fbip-feasibility-2026-06-26.md`): graduated path — (1) **ss38 HAMT maps** = the bounded zero-risk first increment, **FIREABLE NOW** (kills the O(n)-clone tax + the S94 super-linear blow-up; value-positive standalone; **ss37↔ss38 coupling DISSOLVED** — the Set desugar is rep-agnostic, HAMT auto-covers it) → (2) `lin`-annotated in-place → (3) full inferred reuse (later program wave). Model = compile-time uniqueness-directed in-place (NOT Perceus runtime RC, disqualified on a JS host).
   - **The scanning stdlib** (mechanics-beauty #4) — a scrml-native cursor/char-class/span vocab; sequences WITH the lexer build (dogfood).
   - **The lexer-in-scrml build** = the first real Road-B build wave (after prereqs). Approach B (pure-`fn` match-fold over typed enums) — `compiler-reimagining-lexer-slice-2026-06-26.md` is the design.
2. **Fireable lanes** (user-fired): **ss38 (HAMT — the FBIP increment)** · ss32 (auto-await) · ss33 (cleanup) · ss34 (endpoint/SSR survey-first). **ss35 FROZEN** (native-parser superseded by the rewrite; fix only adopter-blockers — `g-mount-hang-rails-dev` is the lone fire-anyway). **ss36 KILLED** (no-tuple). Lineup: `spa-lists/INDEX.md`.
3. **W4 chunk-model debate — banked `dpa-014`.** Boot the dPA to run it (chunk-as-bundle / load-plan / hybrid; 3 voices incl. the forged `code-splitting-bundler-expert`). Feel-of-performance: W3-codegen verified-BUILT (S91); W4 delivery IS the chunk-model decision; **Component-3 (N≥1 interaction) + role projection** = the upstream RS analytical waves that make the prefetch tiers non-empty (gated on those regardless of chunk model).
4. **bug-1 @apply** (user lean `@apply`, banked — needs its own scoping pass); the rotting backlog (16 MED · 13 LOW + Nominal).

## 🎯 Design narrative (IRREDUCIBLE)
- **The compiler-reimagining fork, de-risked to a COMMIT.** S221 surfaced it (the TS native parser "showcases nothing" — crocodile-dundee). S222 ran THREE design slices: **lexer** (clean — flagship for the type/state layer, 0 fundamental gaps), **DG-builder** (HOLDS but WEAKER — representation clean, graph-traversal mechanics merely adequate, the meta-resonance hook inverted), **`<machine>` substrate** (NO primitive — the `enum+match`-fold IS the substrate). Verdict: **narrow-Road-B** (re-imagine the compiler in scrml's type + case-analysis match-fold ladder; human-authored; parity-port framing DROPPED, human-authorship KEPT) is feasible + generalizes + zero fundamental gaps — the prize is REAL but was BOUNDED (beautiful representation, adequate mechanics) **until FBIP**.
- **FBIP is the flagship-maker.** User's question — "what makes the mechanics beautiful WITHOUT compromise (compile-time free)?" → FBIP (pure source, mutate-in-place where the compiler proves single-ownership). De-risked FAVORABLY: scrml is MORE tractable than Koka/Roc (`lin` §35 = an already-built last-use proof · value-acyclicity §59.5 = no cycle-collection · no-value-identity §45.6 = aliasing→liveness). Perceus runtime-RC disqualified (JS host); fit is STATIC. Graduated/de-risked, not a research gamble. "scrml compiles itself, purely and fast" = the unbounded flagship.
- **Limit-primitives held TWICE.** Set landed as a **map-alias** (B2 — no new type; `set[K]`→`[K:bool]`, set-vocab delegating to the shipped `scrml:data` algebra). Tuples **REJECTED** (the to-tuple-or-not DD: the multi-return motivation was already served by records+destructure in the live self-host; the prior-art named-migration trend — TS/Python retrofitted names onto positions — confirmed positional-anonymous is co-location debt; pro-tuple = imported muscle-memory). The user's records-vs-tuples instinct, backed by data.
- **The `±reactivity` algebra (user-derived).** `engine = machine + reactivity` → the machine is the serializable value-core; the `±reactivity` operation IS the snapshot/restore/observe boundary. The independent `<machine>` DD (deliberately NOT fed the algebra) caught that it argues FOR the fold, not a primitive (restore is free in the fold; a guarded `<machine>` would re-break it via the engine write-guard). The PA's lean was backwards — the independent-check methodology paid off.

## 🛟 Recovered anomalies (reasoning)
- **The non-isolated-sPA shared-checkout saga (THE big one).** The ss37 sPA ran NON-ISOLATED in the shared main checkout and switched HEAD to `spa/ss37`. The PA committed §59.12 + ss31 onto spa/ss37 UNKNOWINGLY → `git push origin main` reported "Everything up-to-date" (pushing the unchanged `main` ref). Caught via the S147 coherence check (`origin/main..main` showed 0/4 + HEAD-on-spa/ss37). Recovered by FF-ing the `main` ref to the clean linear spa/ss37 tip + pushing; recurred for the tuple reversal (commit-on-spa/ss37 + recover-main). Resolved when the Set build landed + I `git checkout main` (releasing the shared checkout). **Lessons: (1) `git branch --show-current` BEFORE every commit when an sPA may share the checkout; (2) fire sPAs ISOLATED (own worktree); (3) the PA had the signal — a `chore(spa/ss37)` commit as a parent — and misread it as "sPA committed to main" twice.**
- **Background pushes can silently no-op.** Two background pushes reported exit 0 + "Everything up-to-date" because the `main` branch was unchanged (the work was on spa/ss37). Verify origin ADVANCED, not just exit 0.
- **The within-node allowlist scope gap (ss37).** The dev-agent's "full suite" = the pre-commit SUBSET (excludes top-level `parser-conformance-*`). The TRUE full `bun test compiler/tests/` flagged `examples/34` over-budget; the sPA re-baselined (diagnosed as ORDINARY example-size native-vs-within-node divergence, NOT set-specific — parser is a no-op). The pre-push hook catches it. **Fixture-adding landings need the FULL suite, not the hook subset.**
- **W3-codegen "third stale estimate."** The W3 A-4 splitter was already-BUILT (S91); the SCOPE row-30 "NOT built" was a Rule-4 mis-read (the `:962` comment was about an empty ReachabilityRecord). W1 already-built, W2 a smaller fix, W3 already-built — the recurring stale-estimate lesson.

## Board @ close
**HIGH 0 · MED 16 · LOW 13 · v0.7.0.** Suite 25474/0/214 (the Set push's pre-push full run). Tree clean (only `main` + `spa/ss36`). Coherence 0/0. Worktrees pruned (S83). Maps: project-mapper refreshed at wrap (watermark → `12600217`). Mechanical: delta-log [109]–[126].

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A · **S219 PRIMARY-GOAL** · S219 flogence digest-boot · **S219 deputy-eliminated** (push = plain S147 coherence) · S88/S99/S126 path-discipline · S136 BRIEF archival · S138 R26 · S147 coherence · S215 adversarial-verify · S217 per-user (bryan) · wrap 8-step. **New this session:** the compiler-reimagining RULING (narrow-Road-B); the limit-primitives-held-twice pattern; the **non-isolated-sPA shared-checkout hazard + the FF-recover-main pattern** (fire sPAs isolated; `git branch --show-current` before commits).

## Tags
#session-222 #close #compiler-reimagining-RULED #narrow-road-b-committed #fbip-de-risked-graduated #set-b2-landed #no-tuple-reversed #ss31-landed #limit-primitives-twice #shared-checkout-saga #independent-check-methodology
