# ss34 progress log

Branch: `spa/ss34` (base `5fb41cb9`, post-ss38). Durable output = this branch + the re-integration message.

- **item 4** g-sse-server-keyword (LOW, Bucket-B) — **PARKED-by-design** (user-ruled DD-deferred; no sPA work).
- **item 3** g-rendermap-needs-server-classification (LOW) — **ALREADY-SHIPPED** (S203/spa-ss10, 2026-06-20). `needs-server` cell-state live in the e2e HARNESS (e2e-render-map.test.js:44 GREEN_STATES + render-harness.js:103-130 detector + 9 baseline cells). NOT a compiler item; footprint stale+misframed (S203 ruled the throw a non-gap; no compiler degradation to build). PA: close.
- **item 2** g-tier1-ssr-prerender (MED, survey-first) — **surveyed → BANK-AS-ARC** (no build). Verdict: multi-wave SSR/hydration arc (scrml has NO server HTML render path today); not coupled to W4. Decomposition W1-W4 + spec-ahead flag (§52.8/§52.5 spec-ahead-of-impl; §52.6.1 placeholder is what's built). Survey doc: `docs/changes/g-tier1-ssr-prerender-survey-2026-06-26/SURVEY.md`.
- **item 1** g-endpoint-multi-statement-arm-invalid-js (MED) — **landed-on-branch** SHA `964d1fc5` (FF off base `5fb41cb9`). SCOPED-DOWN to diagnostic-only per SPEC §61.10 (multi-statement lowering = SPEC-deferred future wave; not built). New `E-ENDPOINT-MULTI-STATEMENT-ARM` Error + `isSingleJsExpression` helper + SPEC §34/§61.9/§61.10 + regression test + reproducer. Full suite 18071/0/68. Agent worktree: `/home/bryan-maclee/scrmlMaster/scrml-g-endpoint-multistmt` (branch `g-endpoint-multistmt-arm`) — to clean up at close. Follow-up: `@`-led arm rewriteServerExpr drop-bug (separate, out of scope).
