// for/lift in markup: canonical list iteration (§17.4)
${
    @items: string[] = ["a", "b", "c"]
}
<program>
    <ul>
        ${ for (item of @items) {
            lift <li>${item}/
        } }
    </>
</>
