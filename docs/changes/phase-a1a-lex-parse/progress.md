# Phase A1a — progress (append-only timestamped)

- [00:00 startup] WORKTREE_ROOT = `/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-a07452d6f161942f0`. HEAD `3c9748e`. Branch `phase-a1a-lex-parse` created. `bun install` ok. `bun run pretest` populated `samples/compilation-tests/dist/` (12 samples).
- [00:01 baseline] Run-1: 8719 pass / 43 skip / **2 fail** / 8764 tests (note: total differs from expected 8763 — one extra test ran, likely a test that intermittently registers itself; consistent with timing-sensitive happy-dom suite). Run-2 (back-to-back): 8720 pass / 43 skip / 0 fail / 8763 tests — exactly matches expected baseline. Run-3 (additional safety): also clean. Per rev-3 flake protocol §7.1: ≤3 fails on run-1 + run-2 lands exactly at expected → BASELINE STABLE; proceed. Flake shape: 2 ECONNREFUSED-tail-shaped failures (visible in tail of run-1 output as `code: "ECONNREFUSED" at <anonymous> (node:_http_client:249:24)`), orthogonal to A1a parser scope. Did not capture exact test names on run-1 (output only tailed).
- [00:02 baseline-commit] Baseline progress.md committed (`7b595c0` — pre-commit hook ran clean, 7995 / 33 / 0).
- [00:03 survey] Surveyed `tokenizer.ts` KEYWORDS (lines 55-85) — `reset` is NOT currently reserved (despite brief saying "already partially recognized"); `default` already reserved; `not` already reserved; `pinned` not reserved (correct — it's contextual). Surveyed `ast-builder.js` `kind: "state"` builder at lines 7349-7407 — existing shape carries `stateType`, `attrs`, `children`, `openerHadSpaceAfterLt`, optional `isSubstate`/`parentState`. Probed expression-parser landmarks (no time for full read).
- [00:04 decomposition] Per dev-pipeline PHASE 0.5 doctrine (Pitfall 4 — Context Overflow), the 11-19h scope across 12k LOC is a textbook context-overflow scenario. Drafted `AST-CONTRACTS-AND-DECOMPOSITION.md` instead of charging into a monolithic implementation that would overflow part-way and leave undocumented mid-state. The contracts document fixes the A1b/A1c-facing AST interface (§1) + lexer contract (§2) + sequential 11-step decomposition with per-step file scope, line ranges, and time estimates (§3). PA should dispatch Step 1 as a fresh sub-agent.
- [00:05 surfacing] No source files modified by S60 dispatch. Two artifacts on branch: this `progress.md` + `AST-CONTRACTS-AND-DECOMPOSITION.md`. Both committed before exit. Surfacing back to PA for sub-agent dispatch decision.

## Step 1 — lexer: reserve `reset` keyword

- [01:00 step-1 startup] WORKTREE_ROOT = `/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-ad9075c352f7e9848`. HEAD `47d54d7` (matches expected). Branch `phase-a1a-step-1-lexer-reset` created from clean tree. `bun install` ok (113 packages). `bun run pretest` ok (12 samples). Baseline run-1: 8719 pass / 43 skip / 2 fail (ECONNREFUSED tail-shape — same flake pattern as S60 baseline). Run-2: 8720 pass / 43 skip / 0 fail / 8763 tests — clean, matches expected. Per flake protocol: ≤3 fails on run-1 + clean run-2 = BASELINE STABLE. Proceed.
- [01:01 step-1 survey] `KEYWORDS` set at `tokenizer.ts:55-85` confirmed; `reset` ABSENT. Token-kind label is `"KEYWORD"` (verified via line 862: `KEYWORDS.has(name) ? "KEYWORD" : "IDENT"`). Existing tokenizer test pattern: `compiler/tests/unit/regex-tokenize.test.js` uses `import { tokenizeLogic } from "../../src/tokenizer.js"` + `tokenizeLogic(content, 0, 1, 1, [])` helper.
- [01:02 step-1 placement] Chose insertion point: `tokenizer.ts:67` (within the "scrml built-in functions / modifiers" group alongside `cleanup, upload, debounce, throttle`). Rationale: `reset` is a state-cell-reset builtin per SPEC §6.8.2 — function-shaped builtin invoked as `reset(@cell)`, semantically homogeneous with the existing builtin group. Added a multi-line in-file comment above the line documenting the reservation purpose and downstream Step-6/Step-8 consumers.
- [01:03 step-1 implementation] `compiler/src/tokenizer.ts`: added `"reset"` to KEYWORDS set at line 71 (with explanatory comment on lines 67-70). Created `compiler/tests/unit/tokenizer-reset-keyword.test.js` with 6 tests: §1 expression-position `reset(@x)` → KEYWORD; §2 function-name position `function reset()` → KEYWORD; §3 let-LHS `let reset = 1` → KEYWORD; §4 substring guards `notReset`/`resetFoo`/`myReset` → IDENT; §5 string-literal `"reset"` → STRING (no KEYWORD); §6 position-independence (3 occurrences across stmt/call/block all KEYWORD).
- [01:04 step-1 isolated test] `bun test compiler/tests/unit/tokenizer-reset-keyword.test.js`: 6 pass / 0 fail / 19 expect calls / 29ms. All cases green.
- [01:05 step-1 full suite] `bun run test`: 8726 pass / 43 skip / 0 fail / 8769 tests (run-1 had 1 ECONNREFUSED flake — same shape as baseline). Delta: +6 new tests, +1 file, 0 regressions. Expected pass count post-change: 8720 + 6 = 8726 ✅ exact match.
- [01:06 step-1 init-test note] `compiler/tests/commands/init.test.js` (29 tests including "app.scrml compiles without errors" which compiles the init template's `function reset() {...}`) still passes — Step 1 is lex-only, so KEYWORD-kind `reset` in function-name position parses without error today. **Flag for Step 8 dispatch:** when `E-RESERVED-IDENTIFIER` is added in `ast-builder.js`, the init starter template at `compiler/src/commands/init.js:65` will need its `function reset()` renamed (e.g. to `clearCount` or similar) — otherwise `init.test.js` line 313 will regress. Listing here so the Step 8 sub-agent picks it up; not in Step 1 scope.
- [01:07 step-1 done] All Step 1 DoD gates green: tokenizer modified, 6 new tests added, full suite green with 0 regressions, no `--no-verify` used. Ready to commit + surface.

## Step 2 — parser: state-decl `shape` discriminant + `isConst` + `initExpr` (Shapes 1 + 3)

- [02:00 step-2 startup] WORKTREE_ROOT = `/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-a077ab2d57f30425b`. HEAD `da8f0a1` (matches expected). Branch `phase-a1a-step-2-parser-shapes-1-3` created from clean tree. `bun install` ok. `bun run pretest` ok (12 samples). Baseline run-1: 8725 / 43 / 2 fail (ECONNREFUSED tail-shape, same flake as prior). Run-2: 8726 / 43 / 0 fail / 8769 — exactly the documented baseline. Flake protocol: BASELINE STABLE. Proceed.
- [02:01 step-2 survey — `kind: "state"` builder] Read `ast-builder.js:7300-7408`. The `kind: "state"` builder at lines 7398-7407 produces nodes for the **markup-tag-style state form**: `<StateName attr1=v1 attr2=v2> ... </>`. It runs `tokenizeAttributes` + `parseTypedAttributes` and emits either `kind: "state-constructor-def"` (if `hasTypedDecls`) or `kind: "state"` (otherwise). The `attrs`/`children` carry the opener-attrs and inner block contents. There is **no** `initExpr`/`shape`/`isConst` field today, and crucially **no RHS-expression branch** in this builder.
- [02:02 step-2 survey — what the PRE-BRIEF assumes vs reality] PRE-BRIEF instructs me to extend `kind: "state"` at lines 7398-7407 with `shape`/`isConst`/`initExpr` for Shapes 1 + 3 (e.g., `<count> = 0`, `const <doubled> = @count * 2`). I built a probe (compileScrml-driven AST dump) for the 12 test cases listed in §5.1 of the dispatch brief and observed the following:
  - `${ <count> = 0 }` → AST emits `kind: "logic"` containing `kind: "html-fragment"` with `content: "< count > = 0"`. **No state-decl node is produced.** The `<` is consumed by markup-fragment tokenization inside the logic body.
  - `${ const <doubled> = @count * 2 }` → AST emits `kind: "const-decl"` with empty `name` and `init` strings, plus an `html-fragment` containing `< doubled > = @count * 2`. **The const-decl is malformed**; `<doubled>` is treated as markup, not as the LHS of a derived cell.
  - `${ <form> { <name> = ""; <email> = "" } }` → All collapses to a single `html-fragment` with content `< form > { < name > = "" ; < email > = "" }`.
  - All Variant-C compound forms behave the same way: text in an html-fragment.
- [02:03 step-2 survey — what IS recognized] Probed the @-prefix form (the current scrml syntax pre-v0.next). `${ @count = 0 }` → `kind: "reactive-decl"` with `name: "count"`, `init: "0"`, `initExpr: {Literal}`. Same for `@items = []`, `@config = {...}`, `@x = compute(input)`. There are **~11 `kind: "reactive-decl"` constructions** in `ast-builder.js` (lines 3001, 3015, 3020, 3030, 3040, 3043, 3132, 3135, 3150, 3153, 4735+, 4767+, etc.). These cover: plain `@x = expr`, `server @x = expr`, `@shared x = expr`, typed-annotation forms, sql-init forms.
- [02:04 step-2 survey — `<x>` decl-site does not parse] `<count> = 0` is **not** currently parsed as a state declaration anywhere in the pipeline. The `<` in expression context is treated as either a markup tag opener (for known component/HTML elements) or an html-fragment text token. The block-splitter / body-pre-parser / ast-builder do not currently have a code path that recognizes `<NAME> = RHS` (or `const <NAME> = RHS`) as a state declaration shape.
- [02:05 step-2 surfaced contradiction] **The PRE-BRIEF + AST-CONTRACTS-AND-DECOMPOSITION.md §1.1 + DISPATCH-A1A-BRIEF.md §3.2.1 line 59 ("Already supported; verify no regression") are inconsistent with the observed parser behavior.** The dispatch brief says Shape 1 is "already supported"; the contracts doc says I should "extend the existing `kind: \"state\"` builder at lines 7398-7407"; but the `<x> = expr` syntax is not parsed today and the `kind: "state"` builder serves a different syntactic form (markup-tag-style state opener, not bare state-decl with `=` RHS).
- [02:06 step-2 design choices] Two interpretations:
  - **(A) v0.next migration interpretation:** "Shape 1 already supported" refers to `@count = 0` (current `reactive-decl`), and the v0.next `<count> = 0` form is genuinely NEW parser surface that A1a must introduce. Under this reading, Step 2 is much larger than 3-4h: it needs lexer/block-splitter/ast-builder changes to recognize `<NAME>` as decl-site (not markup tag) when followed by `=`, plus full parser construction of the new state-decl node. Likely needs to be re-decomposed into 3-4 sub-steps.
  - **(B) Rename interpretation:** `kind: "reactive-decl"` IS the v0.next state-decl node, just under an old name; Step 2 is to **extend the reactive-decl builder** (not the markup-state builder) with `shape`/`isConst`/`initExpr` fields, and concurrently introduce the `<NAME>` decl-site form as an alias for `@NAME` decl-site. Under this reading, the contracts doc §1.1 mis-points at lines 7398-7407 and should point at lines 3001-3160 (+ the parallel block at 4735+).
  - **(C) Hybrid interpretation:** Shape 1 means "the @ form for now" — A1a Step 2 just adds `shape: "plain"` / `isConst: false` / `initExpr` (already present) to `kind: "reactive-decl"` and defers `<x>` decl-site recognition to a later step. Smallest scope but doesn't deliver §6.2's user-facing v0.next syntax.
- [02:07 step-2 HALT] Per dev-pipeline doctrine ("if the existing parser structure doesn't have a clean place to attach `shape`/`isConst`/`initExpr` on the `kind: \"state\"` node, surface that as an open question and HALT — do not invent a parallel structure or hack around it") — HALTING. I did not modify any source file. No new test file was committed (only a temporary probe was created and then deleted). Surfacing the contradiction to PA for guidance on which interpretation (A/B/C) is correct, and which target node kind (`kind: "state"` vs `kind: "reactive-decl"`) Step 2 should extend.
- [02:08 step-2 git state] Branch `phase-a1a-step-2-parser-shapes-1-3` exists with HEAD = `da8f0a1` (no new commits beyond branch creation; no source files modified). Working tree clean. Probe file deleted. Tests baseline-stable at 8726/43/0/8769.

## Step 4 — parser: state-decl `shape` discriminant + `structuralForm` (final field)

- [04:00 step-4 startup] WORKTREE_ROOT = `/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-a3e4c35c945107dae`. HEAD `41d0027` (matches expected). Branch `phase-a1a-step-4-shape-discriminant` created from clean tree. `bun install` ok (113 packages). `bun run pretest` ok (12 samples). Baseline run-1: 8756 / 43 / 2 fail (ECONNREFUSED tail-shape — same flake pattern as prior baselines). Run-2: 8757 / 43 / 0 fail / 8800 — exactly the documented baseline. Per flake protocol: ≤3 fails on run-1 + clean run-2 = BASELINE STABLE. Proceed.
- [04:01 step-4 survey — state-decl construction sites] Enumerated all 17 `kind: "state-decl"` construction sites in `compiler/src/ast-builder.js` via `grep`:
  - **`tryParseStructuralDecl` helper (1 site, line 2924)** — produces both Shape 1 (`<NAME>=expr`, `isConst=false`) and Shape 3 (`const <NAME>=expr`, `isConst=true`). Already sets `structuralForm: true`. Step 4 adds `shape: "plain"` for Shape 1, `shape: "derived"` for Shape 3.
  - **`parseOneStatement` legacy @-form (8 sites)**: 3129 (`server @x = sqlInit`), 3134 (`server @x = expr`), 3154 (`@shared x = sqlInit`), 3157 (`@shared x = expr`), 3246 (`@x:T = sqlInit`), 3249 (`@x:T = expr`), 3264 (`@x = sqlInit`), 3267 (`@x = expr`).
  - **`parseStatements` parallel block (8 sites)**: 4886, 4892 (server), 4914, 4918 (@shared), 5016, 5028 (typed @), 5052, 5063 (plain @).
  - All 16 legacy sites need: `shape: "plain"`, `structuralForm: false`, `isConst: false`. Mechanical additive edit at each.
- [04:02 step-4 survey — legacy const @NAME = expr] **Critical finding**: legacy `const @NAME = expr` produces `kind: "reactive-derived-decl"`, NOT `kind: "state-decl"`. Confirmed via probe: `${ const @doubled = @count * 2 }` yields `reactive-derived-decl` node (also at sites 3044, 3046, 5215, 5225 in ast-builder.js). Per brief risk note: "If a separate kind exists, Step 4 may need to extend that too OR document the divergence." **Choice: document the divergence.** Rationale: (1) Step 3 deliberately renamed only `reactive-decl` → `state-decl`; `reactive-derived-decl` was NOT touched. (2) `reactive-derived-decl` is a widely-used kind across ~20 consumer sites (component-expander, reactive-deps, dependency-graph, route-inference, emit-bindings, emit-logic, type-system, types/ast.ts) — folding it into `state-decl` is a separate large rename in scope of a future step. (3) The contracts doc §1.1 invariant says "state-decl carries `shape`"; `reactive-derived-decl` is structurally a `shape: "derived"` state-decl semantically, but until kind-fold lands, treating them as separate AST kinds is correct. (4) Step 4's invariant test asserts `state-decl` carries `shape`, which is satisfiable without touching `reactive-derived-decl`.
- [04:03 step-4 survey — Variant C compound parents] Probed `${ <form> { <name>=""; <email>="" } }` — produces 0 `state-decl` nodes today (compound block doesn't parse as state-decl-with-children). Confirms decomposition row 11: Variant C is deferred to Step 11. Step 4 has no Variant C compound parent state-decl to handle. The `shape` discriminant rule for Variant C compound parents is therefore inert under Step 4 scope — to be exercised when Step 11 lands.
- [04:04 step-4 design choice — per-site vs shared builder] No shared object-spread builder exists for `state-decl` today. The 16 legacy construction sites are all single-line literal object creations with site-specific field combos (sqlNode vs initExpr, server vs shared vs typed vs plain, return vs nodes.push). **Choice: per-site additive edit.** Rationale: introducing a shared builder adds risk + breaks the symmetry of each branch's tight return statement. Mechanical per-site addition of three identical fields (`shape: "plain"`, `structuralForm: false`, `isConst: false`) is lower-risk and matches existing code style.
