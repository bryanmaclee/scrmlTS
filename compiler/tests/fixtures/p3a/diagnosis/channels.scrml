${ @let dispatcherId = "test-user" }

export <channel name="dispatch-board" topic=@dispatcherId>
  ${
    @shared loads:list = []
    server function refreshBoard() {
      @loads = ["a", "b", "c"]
      broadcast({ type: "refresh", count: 3 })
    }
  }
</>
