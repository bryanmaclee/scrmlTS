// short-circuit with is and is not in compound condition
${
    type Status:enum = { Active, Banned }
    let s:Status = .Active
    let name: string | not = "alice"
    if (s is .Active && name is some) {
        let _both = 1
    }
}
<program>
    <p>ok</>
</>
