// Phase 9: encoded variable names in output (§47)
// All JS output should use _<kind><hash><seq> names. Should compile clean.

${
  let userName = "Alice"
  const maxRetries = 3
  @counter = 0
}

<div>
  <p>${userName} (${maxRetries} retries)</>
  <p>Count: ${@counter}</>
</div>
