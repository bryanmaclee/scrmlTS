// Duplicate variant arm — E-TYPE-023
${
    type Status:enum = { Active, Banned }
    let s:Status = .Active
    let x = match s {
        .Active => 1
        .Banned => 2
        .Active => 3
    }
    log(x)
    function log(n: number) { let _ = n }
}
<program>
    <p>ok</>
</>
