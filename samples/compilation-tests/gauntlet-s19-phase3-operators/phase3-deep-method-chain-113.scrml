// deep method chain with arithmetic
${
    const result = [1, 2, 3, 4, 5]
        .filter(n => n % 2 == 0)
        .map(n => n * n)
        .reduce((acc, n) => acc + n, 0)
}
<program>
    <p>${result}</>
</>
