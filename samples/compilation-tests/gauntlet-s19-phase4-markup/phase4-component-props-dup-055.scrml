// Duplicate prop declaration in props-block + root attribute → E-COMPONENT-012.
<program>
    ${
        const Row = <div class="row" id="fixed-id" props={
            id: string,
        }>
            <span>${id}</>
        </>
    }
    <Row id="x"/>
</program>
