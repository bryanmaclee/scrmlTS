// method chain: string.split().map().filter()
${
    const csv = "  a, b ,  c , d "
    const clean = csv.split(",").map(s => s.trim()).filter(s => s != "")
}
<program>
    <p>${clean.length}</>
</>
