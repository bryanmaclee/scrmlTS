// Phase 9: reactive var gets encoded name (§47)
// Verify @var compiles to _scrml_s_<hash> pattern.

${
  @items = ["one", "two", "three"]
  @selected = 0
}

<ul>
  for (item of @items) {
    <li>${item}</>
  }
</ul>
<p>Selected: ${@selected}</>
