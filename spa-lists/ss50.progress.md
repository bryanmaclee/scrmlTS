# ss50 — legacy expression parse/emit correctness — progress log

- **2026-06-27 boot:** list `ss50-legacy-expr-parse-emit.md` read; 2 disjoint MED items scoped —
  item 1 PARSER-stage (`ast-builder.js` arrow-`?{}`), item 2 EMIT-stage (`emit-expr.ts:emitUnary`
  `**`). Confirmed ss52 already merged into main (`7ab86083`) → no emit-expr.ts reconciliation.
  Branch `spa/ss50` created at local main WITHOUT switching the shared checkout; repointed to
  `352d90f6` when main advanced (WRAP commit, no coreFile overlap).
- **2026-06-27 dispatch (PARALLEL — disjoint files):** item 1 + item 2 → two
  `scrml-js-codegen-engineer` agents, isolation:worktree, opus. Briefs archived under
  `docs/changes/{g-arrow-expr-body-sql-truncate,g-unary-of-exponent-arg-paren}-2026-06-27/`.
  Each briefed with explicit file boundaries (parser-only vs emit-only) for parallel safety.
- **Item 1 result** (agent `ac8fa147`, tip `954a7d16`, `ast-builder.js` only +14/-1):
  `collectExpr` depth-0 BLOCK_REF break suppressed when lastTok is `=>` → the concise-body
  arrow's `?{}` is captured into the same escape-hatch as the block-body form (no orphaned `sql`
  sibling / dangling ParseError). **Scope correction:** the symptom was already a clean fatal
  E-SQL-009 (s224 band-aid); issue-#12 Option-B FORBIDS SQL-in-arrow (E-SQL-009 fatal, lowering
  deferred). The brief's "real query in emit" bullet was over-specified (contradicts Option-B) —
  the agent delivered the ACTUAL item-1 scope (parse captures `?{}` → SQL survives to codegen =
  the ss47-gating prerequisite), did NOT implement lowering. Correct call.
- **Item 2 result** (agent `adfb36aa`, tip `31618cb6`, `emit-expr.ts` + new test):
  `emitUnary` wraps a `**` argument of a prefix unary (`-(2**3)`, AST-precedence check); ss21
  left-operand guard intact; 19-test lock. R26 table all pass (emit + node --check + value).
- **2026-06-27 verify+land:** reviewed both diffs (item 1 = minimal `=>` guard; item 2 = the
  `**` wrap, ss21 untouched). File-sets DISJOINT (intersection empty), neither touched the
  M6.5.b.0 allowlist. Landed via one land worktree (under `.claude/worktrees/`), TWO per-item
  sPA commits: item 1 → **`2fca8075`**, item 2 → **`4490b96a`** (tip). Each through the full
  pre-commit suite + gauntlet/browser post-checks GREEN.
- **Item 1:** landed @ `2fca8075`. **Item 2:** landed @ `4490b96a`.
- **Re-integration:** `main...spa/ss50` = `0  2`; `git merge-tree --write-tree main spa/ss50`
  exit 0 → **clean FF-merge** onto current main (`352d90f6`).
- **RUN COMPLETE — 2/2 landed-on-branch.** Agent worktrees `ac8fa147` + `adfb36aa` left intact
  as PA fallbacks. Re-integration message filed.
