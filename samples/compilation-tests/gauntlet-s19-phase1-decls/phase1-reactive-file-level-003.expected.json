{"feature":"@reactive","shape":"file-level-structural","expectedOutcome":"clean","expectedCodes":[],"notes":"§6.1 / §7.6 reactive cells declared at file top level. MIGRATED S192 (bug-12-vkill stage-2): the prior `file-level-bare` form (`@count = 0` outside any host) was non-canonical — bare file-top WRITES parse as inert text (silently dropped) so the `${@count}` reads resolved to an undeclared cell. Now uses the canonical structural form `<count> = 0` inside a `<program>` default-logic host (§40.8 auto-lift). Outcome remains clean; the read-side E-STATE-UNDECLARED fire (S192) correctly would have surfaced the prior bare form."}
