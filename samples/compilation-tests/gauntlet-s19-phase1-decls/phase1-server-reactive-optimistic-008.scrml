// server @var with optimistic write via assignment in client function
<program db="./test.db">
    ${
        server @count = 0
        function bump() {
            @count = @count + 1
        }
    }
    <button onclick=bump()>${@count}</>
</>
