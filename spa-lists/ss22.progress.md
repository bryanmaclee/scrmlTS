# sPA ss22 — progress log (server-fn pipeline)

**Branch:** `spa/ss22` (off origin/main `cf9f1109`; contains ss19 peer-await `538df06d`) · **Worktree:** `../scrml-spa-ss22`

## Boot (2026-06-25 ~2003 MDT)
- Branched `spa/ss22` from `cf9f1109` (== origin/main, aligns with where the harness branches agents). Local main is ahead at `7fa00be8` (PA re-integrated ss21 + ss27, unpushed) but ss22's surface (type-system/emit-server/rewrite/emit-client/route-inference) is untouched by ss21/ss27 → base parity holds.
- dist + node_modules symlinked. Briefs carry the S112 `git merge origin/main` + `538df06d`-ancestor startup step.

## ⚠️ WEEKLY USAGE LIMIT context
The limit was hit during ss21 (~19:00 MDT 2026-06-25); it resets 7am MDT (2026-06-26 07:00, ~11h out). Current time ~20:03 MDT → **limit likely still active.** Strategy: **PROBE with ONE agent (items 1+2)** before fanning out — if it completes with work the limit is clear and I dispatch the rest (items 3,4,5,6); if it fails on the limit, park all + surface to the user.

## Dispatch
| Item | change-id | agentId | status |
|---|---|---|---|
| 1+2 server-fn typer-scope (SSE route + obj-literal key) | ss22-typer-server-scope | ad6bc1c2ead6f5c2e | PROBE dispatched |
| 3 E-CG-001 overfire | ss22-ecg001-overfire | — | HELD (post-probe) |
| 4 peer-in-template unawaited | ss22-peer-template-emit | — | HELD |
| 5 enum-toEnum server-side | ss22-enum-toenum-server | — | HELD (giti-relevant; server R26 mandated) |
| 6 sql-row-protect-leak (LOW) | ss22-sql-row-protect-leak | — | HELD (STOP-if-bigger; data-flow subsystem) |

Landing = cherry-pick onto spa/ss22. **Serialize re-integration with ss26** if it runs (both touch emit-server) — PA-owned.

## CLOSED 2026-06-25 2144 MDT — 5 of 6 RESOLVED, 1 PARKED. Branch tip `22ad3ac8`.
- Items 1+2 `26c45e1e` · Item 3 `8a6ecd60` · Item 5 `ee14f803` (giti, R26✓) · Item 4 `22ad3ac8`. Item 6 PARKED (SPEC §14.8.7).
- spa/ss22 = base `cf9f1109` + 4 fix commits, clean linear, `0 4` vs origin/main → clean PA merge.
- Integrated server-fn sweep: **371/0 across 28 files**; item-5 R26 7/0; item-4 6/0 (items 4+5 coexist in emit-server; items 3+5 in emit-client — region-isolated).
- Re-integration message: `handOffs/incoming/2026-06-25-2144-from-spa-ss22-reintegration.md`.
- ALL 6 agents completed cleanly (no infra failures this wave). Probe-then-fanout worked (limit clear). sPA STOOD DOWN.

## ⚑ GITI FLAG (item 5)
g-enum-toenum-not-lowered-server-side (`56151c37`) unblocks the §14.4.3 server-side DB-coerce idiom (`?{...}.all().map(row => ({...row, status: Enum.toEnum(row.status) ?? row.status}))`). **PA: flag giti on re-integration.**

## Hypothesis-correction pattern (surface to PA)
The footprints' ROOT CAUSES drifted on 3 of 5 worked items even where loci were close — PA should note for future server-fn footprinting:
- Item 2: NOT server-fn object-literal-key → ast-builder return-TYPE parser, language-wide.
- Item 3: NOT stale-snapshot → string/comment-position regex match.
- Item 5: NOT rewriteEnumToEnum-client-only (it's already in serverPasses) → emitExpr structured-AST path has zero toEnum handling, bypassing string passes.
Each agent reproduced empirically + corrected; all preserved safety invariants / region isolation.

## NEW deferred findings (surface to PA)
- **Item 5 client-side companion:** `Enum.toEnum()` is ALSO un-lowered CLIENT-side for a structured `<cell> = Enum.toEnum(...)` decl (same emitExpr-bypass root cause). The "client toEnum works" assumption holds only for the `.map(arrow=>)` escape-hatch + event-handler paths, NOT structured decls. Candidate client-side follow-on.
- **Item 6:** SPEC-deferred data-flow subsystem (see Events).

## Events
- **PROBE SUCCEEDED** (items 1+2, agent ad6bc1c2): limit clear → fanned out items 3/4/5/6. Item 1 `c19d0852`, Item 2 `8e4fe27a`. **Item-2 PA-hypothesis correction:** it's an ast-builder return-TYPE parser bug (inline-struct `-> {…}` leaks fields into the body), LANGUAGE-WIDE (client too), not a server-fn object-literal-key bug. Shared `consumeReturnTypeAnnotation` helper (dedups 4 buggy loops). ast-builder.js hot-path → PA within-node re-baseline at re-integration. Items 1+2 landing as one sPA commit (bg bkylmeeos).
- **Item 6 PARKED** (agent a0cbcc8b, terminal — no commits, clean tree). The STOP-if-bigger governor fired on all 3 triggers. **SPEC §14.8.7 (L8024-8030) normatively DEFERS** the protected-column-projection leak as a "data-flow / server-fn-return follow-on … not a read-site projection check"; agent swept newest-first → no superseding ratification. A name-only check is UNSOUND (AS-aliased protected col = false-negative on a SECURITY check). RED confirmed (existing `sql-row-typing.test.js:97-109` asserts the RED state). **Real fix needs (follow-on, design/SPEC-ratification FIRST):** (1) provenance channel on `<sql-row>` (source `(table,column)`), (2) body-return value-flow (`inferReturnTypeFromBody` only covers object-literal, not bare `return u`), (3) body-aware route gate, (4) new `E-ROUTE-*`/`E-PROTECT-*` diagnostic + SPEC ratification of the static-projection contract.
