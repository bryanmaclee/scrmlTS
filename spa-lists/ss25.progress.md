# ss25 — ast-builder parse + diagnostics (legacy front-end) · progress

Branch `spa/ss25` · worktree `../scrml-spa-ss25` · base origin/main `cf9f1109`. Legacy block-splitter + ast-builder path (NOT native ss28). Conformance-sensitive. (List file ss25-*.md not on origin/main base — created in the unpushed S221 rebuild — so status tracked here.)

## Items
### 1. g-control-flow-in-markup-lift-body-evades-diagnostic (MED) — RESOLVED (verify-then-close, NOT-REPRODUCED)
R26 across 7 shapes (if/for-of × bare-cell/predicate × </>/</p> closers × preceding-markup-sibling × multi-statement lift body): ALL fire E-CONTROL-FLOW-IN-MARKUP; raw `lift` never ships; bare `lift` in markup also compiles clean (no leak). The gate (ast-builder.js ~L1697, BARE_CONTROL_FLOW_IN_MARKUP_RE on a text block) matches any `if/for/while(...){` regardless of `lift` content. The S214/ss15 claim is stale (gate extended since; the `:1518` line in the gap is also stale → real gate ~1697). known-gaps.md → resolved. No code change.

### 2. g-inline-struct-return-type-misparse (MED) — LANDED
- Agent branch worktree-agent-a9b23e9311a723d00, commit 90283146 (base cf9f1109).
- Locus: LEGACY ast-builder.js parseLogicBody (NOT block-splitter, NOT native — the brief's block-splitter candidate was wrong; the splitter doesn't split fn sig/body for ${} logic blocks, parseLogicBody does it token-wise). Root: the return-type-token loop `else if (_t === "{" ... ) break;` broke at the FIRST depth-0 `{` = the inline-struct TYPE opener, not the body. Fix: braceDepth tracking + type-position heuristic (a depth-0 `{` opens a struct TYPE when a type atom is still expected — start, or after a combinator | & , ( < [ — else it's the body), applied across all 8 return-type loops (:-form + ->-form × 4 fn-decl sites). Named/no-return-type byte-preserved.
- Files: ast-builder.js (+128) + parser-conformance-within-node-allowlist.json (login.scrml MISSING-FIELD 52→59, same-land re-baseline) + ast-builder-return-type-skip.test.js (+6).
- Verify: agent full suite 25229/0, adversarial 13/13, browser+TodoMVC pass; sPA R26 — inline-struct-return repro now errors=[] (was E-SCOPE-001).
- ⚠ WITHIN-NODE RE-BASELINE IS AN INCREASE (52→59, single entry): the LEGACY fix made login.scrml parse the inline-struct-return fields CORRECTLY, surfacing genuine divergence vs the STILL-BUGGY NATIVE parser (native: returnTypeAnnotation=undefined + 2 bare-exprs vs legacy's correct 5-stmt body). Justified (not a canary loosening — native lags). 
- NATIVE RESIDUAL → ss28 (cross-ingestion, per brief STOP-if-native): the native parser has the SAME inline-struct-return bug. When ss28 fixes it, the login.scrml MISSING-FIELD allowlist entry should drop back toward 52.
CONFIRMED (R26): `fn f() -> { active: int, name: string } { return {…} }` → E-SCOPE-001 (first field); the return-type struct is mistaken for the body, splitting `return active;` off as a dangling block. Named return type = workaround. Dispatched (legacy block-splitter/ast-builder fn-signature/body disambiguation; STOP→ss28 if native). Conformance-sensitive → full suite + within-node re-baseline in the same land. BRIEF archived.

### 3. bug-75 after-> engine :-shorthand (LOW, KEEP-OPEN user-ruled S177) — RESOLVED (verify-then-close, NOT-REPRODUCED)
R26: the after-`>` engine `:`-shorthand now WARN-COMPILES — W-COLON-SHORTHAND-LEGACY-PLACEMENT fires, errors=[], NO hard-fail E-STRUCTURAL-ELEMENT-MISPLACED. Byte-identity verified vs inside-opener: client.js byte-identical; static HTML identical modulo filename (both `<div data-scrml-engine-mount="phase">idle text</div>`); the `:`-body renders. The S177 user KEEP-OPEN condition (keep open UNTIL warn-compile byte-identical) is MET. known-gaps.md → resolved. ⚠ user-ruled — recommend close pending user confirm. No code change.

## Status
- [resolved-verify-then-close] item 1 (NOT-REPRODUCED) · item 3/bug-75 (NOT-REPRODUCED, user-ruled → confirm)
- [landed-on-branch] item 2 (90283146) — native twin → ss28

## Note — corpus ran ahead (3rd-4th verify-then-close this sweep)
2 of 3 ss25 items were already-fixed on cf9f1109 (the S221 list captured stale claims). Same pattern as ss27 #1 (bug-19) + ss19 stale gaps. The R26 verify-before-claim (reverse: NOT-REPRODUCED) discipline caught it — no wasted re-fix dispatches.
