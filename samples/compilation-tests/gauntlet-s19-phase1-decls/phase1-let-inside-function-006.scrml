// let inside function body
${
    function tally(items) {
        let total = 0
        for (const item of items) {
            total = total + item
        }
        return total
    }
}
<p>${tally([1,2,3,4])}</>
