# ss42 — named-machine undeclared-read (Model 1) · progress

- **Items 1-3** → `landed-on-branch` on `spa/ss42` (one dispatch, scrml-js-codegen-engineer `agent-a1715789`, opus). Item 4 stays `deferred — needs own ruling` (not built, per list).
  - **Item 1** (`1ab17ce3`): narrow the S192 machineRegistry pre-bind. Sweep = ~0 blast radius (compiled corpus + flogence clean; 17 test fixtures referenced, 0 newly-fired, 0 migrations). **Discriminator CORRECTED** (cookbook-vs-empirical): list's literal "derived-only" would regress non-named `<engine for=Type>` §51.0.C auto-cells — correct Model-1 discriminator is `hadNameAttr && !isDerived` (skip pre-bind only for NAMED non-derived; keep §51.9 derived + non-named for=Type). Threaded `MachineType.hadNameAttr`.
  - **Item 2** (`061d48a7`): route match `on=@X` through the read-side walker — done in the typer's `match-block` case (not emit-match.ts; the diagnostic belongs in the typer). Undeclared `on=` now fires E-STATE-UNDECLARED.
  - **Item 3** (`60b600fb`): W-ENGINE-INITIAL-MISSING misfire suppressed for §51.3 named + `<machine>`-keyword arrow forms; NOT over-suppressed (genuine non-named state-child engine missing `initial=` still fires).
  - **Regression test** (`b2b2d804`, sPA-requested): `compiler/tests/unit/named-machine-undeclared-read-ss42.test.js`, 10 cases, REAL compiled source, cross-stream diagnostic-partition helper. **Non-vacuous** (agent neutralized the narrow → cases 1+2 failed, then restored). Independently re-run by sPA: 10/0.
  - Files: `type-system.ts` (+87), `symbol-table.ts` (+16/-1), new test (+270). No allowlist rebaseline (0 within-node shift). True full `bun test compiler/tests/` **25519 pass / 0 fail**.
  - sPA verify: new test re-run 10/0; FF-merged agent branch (4 verified commits) onto spa/ss42; bookkeeping commit re-ran the full gate.
  - **Landing under cross-session contention:** sibling sPA ss40 (`scrml-spa-ss40`) + its agent running concurrently; memory-gated the bookkeeping commit (>6G) per S219.

**End-state:** items 1-3 = landed-on-branch; item 4 = deferred. 0 parked, 0 dropped.
**⚠ type-system.ts** is a hot file — PA intersect at re-integration vs any concurrent typer lane (S211).
