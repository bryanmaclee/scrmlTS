# ss18 — endpoint-primitive — progress log

Branch `spa/ss18` (base `45182694`, worktree `../scrml-spa-ss18`). Sequential waves W2→W5.

## Decisions (sPA, S219)
- **Native-parser mirror SKIPPED** for `<endpoint>` (all waves) — follows the direct §60 `<api>` precedent (api is native-UNMIRRORED; native parser is not the production path; S162 conditional). Flagged to PA at re-integration. If the native parser becomes the production path, a follow-up mirror is needed.
- **Per-wave focused tests** (not all-in-W5) — follows the `<api>` precedent (`api-decl-parser.test.js` landed with W2, `-typer` with W3, `-codegen` with W4). W5 keeps the integration + example + flogence conformance. Minor deviation from the list's literal "tests in W5"; correct engineering discipline + per-wave verification/lock.

## Timeline
- **W2 (parser)** — ✅ **LANDED `8253f562`** on `spa/ss18`. Agent tip `b9478497` (base 2a4bf8af, stale) cherry-pick-`-n`-squashed → single sPA commit (3-way merge auto-resolved SPEC.md vs sibling 45182694's W-INPUT-STATE row; preserved). Files: block-splitter.js (+20), ast-builder.js (+188), SPEC.md §34+§61.9 (+9/-3), endpoint-decl-parser.test.js (+247, 14 tests). Pre-commit full-suite hook passed (commit object written ⟹ hook exit 0). R26 independent: valid 3-arm endpoint-decl node {path,method,acceptsRaw,arms} incl parenthesized-payload binding via parseMatchArms reuse; all 3 codes fire with correct result.errors partition. Node shape confirmed: `{kind:"endpoint-decl", path, method, acceptsRaw, arms: MatchArmEntry[]}`.
  - **Env notes:** worktree dist gap (memory spa_worktree_gitignored_dist_gap) — symlinked `samples/compilation-tests/dist` → main. Foreground commit hit the 5-min wall (lingering test child held the pipe) but the commit landed; full-suite hook genuinely slow in this env.
  - **Main drift:** main advanced to `26ffea4e` (S219 WRAP, docs-only) during the session — spa/ss18 cleanly diverges `1 1`; PA reconciles at re-integration (26ffea4e touches none of my files).
- W3 (typer) — `in_progress`. Consumes the W2 `endpoint-decl` node. Pre-scoped: `checkApiDeclarations` (type-system.ts L18697, called ~L19621) template; `checkEnumExhaustiveness` (L13520) for E-ENDPOINT-NOT-EXHAUSTIVE; `checkEndpointTypeRef`/E-TYPE-UNKNOWN-NAME reuse for undeclared accepts=; E-ENDPOINT-ACCEPTS-NOT-ENUM on non-enum resolve. **W3 agent must base off spa/ss18 (has W2 ast-builder) — dispatch from the sPA worktree CWD.**
- W3 (typer) — `in_progress`. Dispatched `scrml-js-codegen-engineer` (isolation:worktree, opus) from MAIN CWD (CWD won't pin to the sPA worktree — post-dispatch reset, memory cwd_reset_post_dispatch). Agent briefed to `git merge spa/ss18 --no-edit` at startup to pull W2 (fast — uses pre-merge-commit not the full-suite pre-commit). Brief: `BRIEF-W3.md`. Landing plan: cherry-pick the agent's W3 commits (AFTER its merge commit) onto spa/ss18 — clean, since main's 26ffea4e/31d01451 are docs-only (don't touch type-system.ts/SPEC/tests).
  - **Main drift continues:** main now `31d01451` (S220 boot bookkeeping). Still docs/maps-only; spa/ss18 typer files == main's. PA reconciles at re-integration.
- W4 (codegen) — pending W3. **Pre-scoped landmarks:** `emit-server.ts:generateServerJs` (L741, the route-handler emitter — `async function _scrml_handler_*(_scrml_req)`, `new Response(JSON.stringify(...))`, registered via `__ri_route_*`+`routes`+`fetch`); `emit-parse-variant.ts:emitParseVariantCall` (L163, reuse for the `accepts=` body decode); `emit-variant-guard.ts` (`extractPayloadBindingsFromAttrs` L1251 + the tag-switch skeleton). **NUANCE:** `emitVariantGuardedRender` (L886) is MARKUP-render — an endpoint arm dispatch is VALUE→JSON (arm body = code expr returning a value to envelope), NOT a render. Reuse the payload-binding + variant-switch skeleton, NOT the HTML render path. Route-inference: explicit `<endpoint>` ⇒ emit handler, skip data-layer ser/deser + CSRF gate (§61.6/§61.7; route-inference.ts csrf at L289/L3790). Deep-scope W4 after W3 lands.
- **W4 (codegen)** — ✅ **LANDED `402fccb6`** on `spa/ss18`. Agent W4 commits a3d00455/9a37c84d (on merge eb15e256) cherry-pick-`-n`-squashed. Files: emit-server.ts (+239 endpoint handler + route record), type-system.ts (+6 acceptsEnum annotation), endpoint-decl-codegen.test.js (NEW 10), endpoint-decl-parser.test.js (W2 no-emission test updated — superseded), SPEC §61.5 (+normative schema). R26 independent: /fsp endpoint → .server.js handler + parseVariant decode + 3-variant dispatch + __ri_route_endpoint_N at verbatim path/method in routes/fetch + 200/400 envelopes + no CSRF; client clean; node --check clean. No main leak (verified — the 3 ast-builder "endpoint-decl" hits are §60 api grammar comments, not W2). **§61.5 default-envelope decision (FLAG PA):** direct-serialize success (200, no wrapper), 204 self-closing, compiler-owned {error:{kind,message}} 400 decode-failure — §61.5 delegated the schema to W4.
  - **Agent corrected the brief:** the server-emission gate is in emit-server `generateServerJs` (`return ""` early-exit ~L976), NOT route-inference (added `!_hasEndpointDecls`); and W4 added the `acceptsEnum` annotation W3 hadn't.
  - **COMMIT CONTENTION:** the commit's full-suite pre-commit hook was OOM-killed by a CONCURRENT session (`d3c8e1f3`) running 2 stuck commits to MAIN + multiple bun-test runs (parallel-sPA thrash). Did NOT kill their work; memory-gated retry → landed on the first >6GB-free window. Env note for PA: concurrent sessions committing to main contend with sPA worktree commits.
- W4-archived — original architecture brief below:
- W4 (codegen) — landed (see above). Brief: `BRIEF-W4.md`. **Architecture decided:** emit-server `generateServerJs` deep-walks fileAST for endpoint-decl → emits `_scrml_endpoint_<id>` handler (parseVariant decode → variant switch dispatch → direct-serialize envelope, NO CSRF block) + `export const __ri_route_endpoint_<id>={path,method,handler}` (auto-collected by the L2466 routes/fetch regex); route-inference gate includes endpoint-presence for .server.js emission; client-skip. **§61.5 default-envelope DECISION (flag to PA):** direct-serialize the arm return value (200) + compiler-owned `{error:{kind,message}}` on decode-failure (400); no magic wrap → author owns the success wire by return-value (JSON-RPC = convention not baked-in mode). No new error codes. Landing cherry-pick clean (main drift — now `23601835`, S220 Ryan-battery ingest — doesn't touch emit-server/route-inference/emit-client/emit-parse-variant).
- W5 (tests + example + conformance) — pending W4. **Pre-scoped + SCOPE SPLIT:**
  - IN SCOPE (dev-agent): integration tests (emit-server end-to-end) + `examples/33-endpoint.scrml` (template `examples/32-external-api.scrml`) + a conformance PROOF re-hosting flogence's 11 `fsp-wire-smoke` assertions against a scrml-served `/fsp` `<endpoint>` (as a scrml-repo test) + flip the §61.10 Nominal banner.
  - **ESCALATE to PA (cross-repo):** flogence retiring `../flogence/scripts/fsp-wire.ts` as the PRODUCTION transport — a flogence-repo production change beyond the ss18 compiler ingestion. flogence IS at `../flogence` (fsp-wire-smoke.ts present). The conformance PROOF (scrml-side) is in scope; the flogence production migration is the PA's cross-repo call.
  - ✅ **LANDED `9f9f9984`** (scrml-side scope). Agent W5 commits dac7e805/ce74dfd1/9bbb203b (merge 0b0c4aff) cherry-pick-`-n`-squashed. examples/33-endpoint.scrml (compiles, /fsp route, client-clean) + endpoint-conformance-integration.test.js (12/12) + §61 banner Nominal→Implemented + SPEC-INDEX regen + parser-conformance allowlist. R26: integration 12/12 + example emits route + no client leak. Memory-gated commit (cross-session OOM contention persists).

## RUN COMPLETE — ss18 fully dispositioned
All 4 waves LANDED on `spa/ss18` (tip `9f9f9984`): W2 `8253f562` · W3 `6d17e02b` · W4 `402fccb6` · W5 `9f9f9984`. Re-integration message sent to `handOffs/incoming/`. Residuals for PA: (1) HIGH §61.2 canonical-arm reachability gap, (2) MED multi-statement bare-body arms, (3) §61.5 envelope-decision confirm, (4) flogence cross-repo conformance escalate. PA re-integrates spa/ss18 → main + reconciles the `7 4` divergence (all main drift is bookkeeping; none touches endpoint files).
