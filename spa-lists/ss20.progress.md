# sPA ss20 — progress log

**Branch:** `spa/ss20` (off main HEAD `bb1f2592`, contains ss17 landing) · **Worktree:** `../scrml-spa-ss20`

## Boot (2026-06-25)
- Branched `spa/ss20` from **local main** `bb1f2592` (NOT origin/main `26ffea4e` — origin is 4 commits behind; the ss17 per-item-emitter landing `72b52b6d` these items build on is local-only).
- dist symlinked from main (S209 gap); node_modules symlinked.
- Footprint correction: item 6 locus is `collect-class-names.ts::collectClassNamesFromAst`, NOT `tailwind-classes.js` (that's the CSS generator).

## Dispatch — all 5 work-units parallel (scrml-js-codegen-engineer · worktree · opus)
| Item | change-id | agentId | status |
|---|---|---|---|
| 1 g-if-guard-inner-effect-not-gated (HIGH) | ss20-if-guard-effect-gate | a11b257ef2e6184c2 | dispatched |
| 2 g-compound-bind-value-not-two-way (HIGH) | ss20-compound-bind-subfield | ab2acf68f12bcebe7 | dispatched |
| 3 g-each-mount-form-submit-no-preventdefault (MED) | ss20-each-submit-preventdefault | a3db8b1f4374f37ce | dispatched |
| 4+5 emit-lift markup-text interp (MED+LOW) | ss20-emit-lift-markup-text-interp | a36315fb95818d7c9 | dispatched |
| 6 g-each-match-body-class-literal (MED) | ss20-class-extractor-each-match-body | a1c556a75511d82d3 | dispatched |

5 disjoint primary files (emit-html · emit-bindings · emit-each · emit-lift · collect-class-names) → parallel-safe; sPA serializes landings. Watch for shared test-baseline collision at land time (S211).

## ⚠️ BASE-STALENESS FINDING (affects ALL landings)
The `isolation:worktree` harness branched every agent from **`26ffea4e` (origin/main, PRE-ss17)**, NOT `bb1f2592` (the brief's stated base). The brief said "Base: bb1f2592" but did not force a `git merge main` startup step (S112 class). Consequence:
- Agent commits' tree-diff vs bb1f2592 shows the entire ss17 landing as spurious "deletions" — those are just ss17 code absent from the agent's older base, NOT agent removals. The COMMIT itself only touches its real files.
- **LANDING MECHANISM: cherry-pick (3-way merge), NOT wholesale file-delta.** File-delta of an emit-each.ts (or any ss17-touched file) would clobber ss17. Cherry-pick's 3-way merge (base = agent parent 26ffea4e) preserves ss17 + applies the agent change.
- Items 1/2/6 (emit-html · emit-bindings · collect-class-names) — ss17 never touched these files → identical in 26ffea4e and bb1f2592 → clean either way, but cherry-pick for consistency.
- **Item 4+5 (emit-lift)** — ss17 did NOT modify emit-lift.js, BUT its each-side workaround interacts; run the ss17 each browser tests on spa/ss20 after landing to confirm no double-lowering.

## Landings
- **Item 3 LANDED** `a41d3227` (cherry-pick of agent 24809d55 onto spa/ss20). Coherence: parent=bb1f2592, base+1, ss17 markers + preventLine both present, tree clean. Message↔content coherence re-verified (item-6 agent flagged a scratchpad commit-message race — see below; item-3 landing UNAFFECTED, own-authored sPA message + clean 2-file cherry-pick).
- **Item 2** (`g-compound-bind-value-not-two-way`, HIGH) — agent `8b3f3637` LANDED (NOT parked): source field cells exist as flat dotted keys (`loginForm.email`); retarget bind read/write to source leaf via `collectCompoundLeafTargets`. Touched emit-bindings.ts + emit-variant-guard.ts + new test. RED 1/5→GREEN 6/0. **LANDED `1f03f5fe`** (base+2, coherent).
- **Item 4+5** (emit-lift markup-text interp) — agent `cb439571` COMPLETE, verified. emit-lift.js TEXT-child branch lowers un-split `${...}` reactively, **gated `!currentLiftReconcileCtx()`** so the each path (ss17) is byte-identical (double-lowering concern RESOLVED). Item 5 closed by the single item-4 fix. Blocking gate 17744/0, browser 505/0. Only emit-lift.js+test → no emit-variant-guard collision with item 2. **Pending cherry-pick** (blocked on item-2 index lock).
- **Item 6** (class-extractor) — agent `035df17f` COMPLETE. **FOOTPRINT MISS — no TS bug:** `collectClassNamesFromAst` already handles each/match bodies (since S212). Real root cause is the **self-host** collector (`section-assembly.js ~L2113-2118`, scanClassesFromHtml-only) — B4-deferred/forbidden. Agent delivered a regression-guard test only (8 tests, no source change). Cherry-picked -n; **commit running in background** (bpqeofhf1).
- **Item 1** (if-guard, HIGH "core fix") — agent `1f65cb8d` COMPLETE. Shape-(a) early-return gate; extracted `computeDisplayToggleCondition` so the gate predicate is BYTE-IDENTICAL to the display-toggle (lockstep, no re-lowering). Touched emit-html.ts (ifGuardStack + stamp ifGuard onto descendant interp LogicBindings) + emit-event-wiring.ts (shared helper + gate initial-render + effect body) + binding-registry.ts (doc ifGuard field) + new 9-case browser test. RED 4/9→GREEN 9/9; core gate 17745/0 (0 regressions). Files DISJOINT from all landed items → clean cherry-pick. Agent merged main FF to bb1f2592 (has ss17). **Pending cherry-pick** (blocked on item-4+5 index lock bc8n1zjij).

## More findings
4. **if-chain branches (`if=`/`else-if=`/`else`) in display-mode** (from item 1, DEFERRED) — same latent null-interp gap if a chain branch carries reactive interpolation over a null cell; separate `if-chain` node kind, brief-excluded. Flag for follow-on if it surfaces.

## Main is PA-active (re-integration awareness)
Main working tree carries the **PA's uncommitted endpoint-primitive WIP** (`docs/changes/endpoint-primitive-2026-06-25/`, emit-server.ts, ast-builder.js, block-splitter.js, type-system.ts, SPEC*, endpoint-* tests, examples/33-endpoint.scrml) + main HEAD `d800b79f`. NONE overlap spa/ss20's surface (emit-html/event-wiring/bindings/variant-guard/each/lift/collect-class-names + tests) → clean merge. sPA touched ONLY its own spa-lists/ss20 tracking files in main's tree.

## CLOSED 2026-06-25 1408 — all 6 items closed, branch tip `04228356`
- Item 1 LANDED `04228356`; Item 2 `1f03f5fe`; Item 3 `a41d3227`; Item 4+5 `61f7b2b2`; Item 6 `acc6ef0a` (guard-only, footprint-miss).
- spa/ss20 = bb1f2592 + 5, clean linear. Divergence vs main d800b79f = `1 5` (d800b79f no codegen overlap → clean merge).
- Integrated browser verify: 4 new render-fix tests 31/0 together; each+lift+markup sweep 193/0 across 29 files (ss17 intact, no double-lowering).
- Re-integration message: `handOffs/incoming/2026-06-25-1408-from-spa-ss20-reintegration.md`.
- NO parked items. 4 deferred findings + footprint corrections in the message. sPA STOOD DOWN.

## Auth-leak note (checked, resolved)
Item-1 agent saw `auth-graph.ts`/`generate.js`/4 auth tests transiently modified in ITS worktree (pages/auth/login→pages/login error-string change, unrelated to ss20) — attributed to sibling working-tree noise via the shared checkout; agent restored to HEAD, its commit verified = exactly its 4 files. **Main working tree re-checked: NO auth/generate leak present.** Resolved; no PA action needed.

## Main advanced under sPA
Local main moved `bb1f2592 → d800b79f` (s220 "Nominal currency-verify" bookkeeping) DURING the dispatch. d800b79f touches NONE of the 5 ss20 codegen files → spa/ss20 (based bb1f2592) re-integrates cleanly. Noted for PA.

## NEW deferred findings (surface to PA at re-integration)
1. **`<errors>` anchor DOM not reactively clearing** (from item 2) — separate `<errors>`-emitter/runtime gap: `_scrml_reactive_subscribe` on a DERIVED cell never fires (derived recompute fans out only effects via `_scrml_trigger`). Item-2 primary symptom fixed; this secondary one out of scope.
2. **`<tableFor>` single-column slot renders literal `${@row.name}`** (from item 4+5) — same bug class as 4/5 but on the EACH path (brief-excluded). MASKED by a weak assertion `js.toContain("row.name")` at `r28-bug-2-tablefor-column-row-access.test.js:207` (passes on the literal substring). Candidate follow-on ss20 item + strengthen that test to value-assert.
3. **Self-host class collector** (from item 6) — `compiler/self-host/cg-parts/section-assembly.js ~L2113-2118` AST-walk gap = the actual flogence #3 "squashed bubbles" root cause. Self-host/B4-deferred.

## Process: scratchpad commit-message race
Sibling parallel agents share the session scratchpad → item-6's `commitmsg.txt` was clobbered by item-3's (item-6 self-recovered via amend). New memory `feedback_parallel_dispatch_shared_scratchpad_race`. sPA landings unaffected (own-authored messages).

