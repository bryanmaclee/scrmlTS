${
    type Color:enum = { Red, Green, Blue, Purple }
    <c>: Color = .Red
    const <isWarm> = match @c {
        .Red | .Purple => true
        .Green | .Blue => false
    }
}
<program>
    <p>warm: ${@isWarm}</p>
</program>
