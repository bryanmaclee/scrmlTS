# scrml — Session 223 (CLOSE)

**Date:** 2026-06-26. **Profile:** A — FULL. A high-throughput orchestration session under the S219 primary-goal posture: **W4 chunk-model RATIFIED · `@apply` §26.8 W1 authored · SIX sPA lanes landed** (ss38·ss32·ss41·ss39·ss34·ss33), one parked "design ruling" dissolved under SPEC grounding, and a four-way `emit-client.ts` contention reconciled per-base.

> Mechanical stream → `handOffs/delta-log.md` [128]–[148]. Board: `bun scripts/state.ts`. Boot digest: `bun ../flogence/scripts/digest.ts scrml --fresh`. This carries the IRREDUCIBLE (narrative · open threads · recovered anomalies).

## 🚨 NEXT-START
Boot Profile A. Board @ close: **HIGH 0 · MED 14 · LOW 9 · Nom 8 · v0.7.0 · suite 25489/0/214.** Coherence 0/0 (everything pushed). NO deputy (S219). Maps refreshed at wrap → watermark `6988c426`. Worktrees pruned at wrap.

## ⏸️ OPEN — S224 (priority order)
1. **🎯 THE COMPILER-REIMAGINING PROGRAM — narrow-Road-B (RULING.md S222).** **ss38 HAMT maps LANDED = FBIP increment-1 DONE** (the bounded zero-risk first step; S94 blow-up fixed). Next FBIP increments: (2) `lin`-annotated in-place → (3) full inferred reuse (later program wave). The scanning stdlib (mechanics-beauty #4) + the lexer-in-scrml build (Approach B, `compiler-reimagining-lexer-slice-2026-06-26.md`) = the first real Road-B wave (after prereqs). The native-parser TS transition stays FROZEN.
2. **Fireable sPA lanes:** **ss40** (`@apply` W2 build — implement §26.8: recognize the directive in the `#{}` rule parser + expand in `emit-css.ts:renderCssBlock` + the 3 `E-APPLY-*` codes + flip the Nominal banner; survey-first) · **ss42** (named-machine undeclared-read, Model 1 RULED — narrow the S192 pre-bind to derived-only [corpus-sweep FIRST] + route match `on=` through E-STATE-UNDECLARED + fix the W-ENGINE-INITIAL-MISSING misfire; survey-first). ss38 fired the FBIP path — next increments are a Road-B program wave, not an sPA. Lineup: `spa-lists/INDEX.md`.
3. **Banked/parked:** `g-tier1-ssr-prerender` (ss34 item-2 — a SURVEY-banked multi-wave SSR arc; survey at `docs/changes/g-tier1-ssr-prerender-survey-2026-06-26/`) · `g-endpoint-at-led-arm-trailing-expr-dropped` (NEW MED, ss34 surfaced — silent trailing-expr drop on `@`-led endpoint arms; needs its own pass) · the optional unbound-named-machine lint (ss42 item-4, needs its own ruling). The rotting backlog (MED 14 · LOW 9 + 8 Nominal).

## 🎯 Design narrative (IRREDUCIBLE)
- **W4 dpa-014 RATIFIED — ship B (load-plan over `_scrml_modules`).** Pure-A dominated: re-partitioning the registry spends the content-addressed file-key contract (a tree-shaking WALL, conceded by the A advocate) for a DCE win the contract won't grant. MODEL-now / ACTIVATION-gated-on-Component-3. The durable insight: *a content-addressed self-registering module registry's file-key IS the chunk boundary; the min-new-primitive tiered-loading model is a MANIFEST, not a bundler; and the prefetch trigger must be a programmable predicate* (`[S223/dpa-014]`).
- **`@apply` = bug-1's last sub-arc, de-risked small.** The key finding: scrml ALREADY has the `#{}`/`<style>` CSS surface + per-utility resolver (`getTailwindCSSWithDiagnostic`) + the §26.7 var()-composition — so `@apply` is a directive that reuses all three, NOT a new pipeline. The `#{}`-suppression false-positive was already solved; @apply adds the composition ergonomic. safelist (dynamic generation) = a separate deferred problem.
- **ss32 item-2 — the SPEC dissolved a 4-way "design fork" into a non-bug.** The survey framed "make top-level derived cells live" as a language-wide semantic change. But SPEC §6.2-Shape-3 / §6.6.2 / §6.6.7 MANDATE `const <derived>` cells live (lazy-pull + `_scrml_derived_declare`/`subscribe`; the one-shot stub is retired + throws). A PA probe confirmed `const <derived>` is already race-free; the survey's repro was a plain MUTABLE cell (spec-conformant one-shot). **fork(g)** (the `!{}`-dead-handler) was the only real bug — fixed in ss41. *Lesson: ground a parked "designer-axis ruling" against the SPEC before presenting — Rule 4 / don't-soft-classify.*
- **ss39 item-3 RULED Model 1.** `@pm` is NOT a legal read of a non-derived named machine (§51.3.3 mandates a separate `@var: MachineName` binding; §51.0.B says `name=` sources no auto-var; §51.4 → no canonical auto-cell). Auto-emit-init is SPEC-forbidden. The fix is the inverse: narrow the over-eager S192 pre-bind so `E-STATE-UNDECLARED` fires loudly. Scoped as ss42.

## 🛟 Recovered anomalies (reasoning)
- **The 4-way `emit-client.ts` contention (the big one).** ss32 · ss41 · ss33 (+ ss39/ss34 stale-views) all touched `emit-client.ts` from DIFFERENT bases (ss41 on current main; ss33 on the oldest 69cee28b). A blind file-delta of any older branch would have REVERTED ss32's `.catch` / ss41's fork-g. Resolved by **landing in base order** (ss41 clean file-delta first; ss33 via `cherry-pick` of its specific functional commits so the read-prune hunk @~2280 3-way-merged against ss32/ss41 @~1988-2166 — disjoint regions, clean). Every landing verified the prior fixes intact (grep counts) post-commit. **Lesson: with N lanes on one hot file, cherry-pick the specific per-item commits, never wholesale file-delta a stale-based branch.**
- **The deep stale-view filter.** Branches based pre-ss38/ss32/§26.8 show ALL the landed work as "reversals" in `git diff main..branch`. ss39's diff listed 29 files (most were my PA-pile + ss32/ss38 reversals); only its 6 genuine src/test + 4 BRIEFs were pulled. **Always `git diff --name-only` + verify each file is genuine-vs-stale-view before file-delta.**
- **ss34 ran NON-ISOLATED (S99/S222 hazard).** An ss34 sPA ran in the PA's shared checkout — switched HEAD to spa/ss34, leaked its list-status edit + BRIEF + tier1-survey into main's working tree, committed item-1 to spa/ss34 via a careful ref-only FF. **Verified zero corruption** (my §26.8 + every landing intact; the sPA was careful not to touch the dirty working tree). Recovered via `git checkout main` + per-hunk cherry-pick of its §61 (auto-merged against committed §26.8). **Lesson: fire sPAs ISOLATED (own worktree).**
- **SPEC §61 vs §26.8 same-file landing.** ss34's §34/§61 and my uncommitted §26.8 both lived in SPEC.md. Consolidating (committing §26.8 first) made ss34's landing a clean `cherry-pick` 3-way-merge (different sections) instead of a risky per-hunk against uncommitted work. **Committing the PA pile before reconciling same-file sibling work was the right call.**

## Board @ close
**HIGH 0 · MED 14 · LOW 9 · Nom 8 · v0.7.0.** Suite 25489/0/214. Coherence 0/0 (scrml + scrml-support both pushed). Maps watermark `6988c426`. Worktrees pruned. Mechanical: delta-log [128]–[148].

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A · **S219 PRIMARY-GOAL** · S219 flogence digest-boot · S219 deputy-eliminated · S88/S99/S126 path-discipline · S136 BRIEF archival · S138 R26 · S147 coherence · S215 adversarial-verify · S217 per-user (bryan) · wrap 8-step. **New this session:** the 4-way-hot-file cherry-pick-per-commit reconciliation; the deep-stale-view filter discipline; ground-a-parked-design-ruling-against-SPEC-first (item-2 non-bug).

## Tags
#session-223 #close #w4-dpa-014-ratified #apply-26.8-w1 #six-lanes-landed #ss38-hamt-fbip-increment-1 #ss32-ss41-auto-await-error-arms #ss39-parse-fixes #ss34-endpoint-diagnostic #ss33-runtime-minimality #item-2-not-a-bug #item-3-model-1 #emit-client-4-way-reconciliation #fire-spas-isolated
