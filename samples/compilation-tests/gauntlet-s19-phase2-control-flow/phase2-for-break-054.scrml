// break inside for-of
${
    let xs = [1, 2, 3, 4, 5]
    let found = -1
    for (let x of xs) {
        if (x == 3) {
            found = x
            break
        }
    }
    log(found)
    function log(n: number) { let _ = n }
}
<program>
    <p>ok</>
</>
