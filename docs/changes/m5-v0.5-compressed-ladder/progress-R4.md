# progress-R4 — M5-swap Unit R4, SPEC §34 reconciliation

Append-only. Timestamps approximate (S117, 2026-05-21).

- S117 start — worktree `/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-abd4a4f8b521eb4de`. Startup verification: pwd + repo-root match; `git merge main` brought brief in clean; `bun install` ok; baseline `bun run test` = 18,102 pass / 0 fail / 169 skip / 1 todo / 738 files (matches brief; transient 2-fail flake cleared on the briefed re-run).
- S117 — enumerated native-parser diagnostic codes: 30 `E-EXPR-*` + 35 `E-STMT-*` + 1 `E-MARKUP-VALUE-UNCLOSED` = 66 not in §34; 8 already in §34 (`E-ASYNC/AWAIT/FOR-AWAIT-NOT-IN-SCRML`, `E-UNQUOTED-DISPLAY-TEXT`, `E-CTX-001`, `E-CTX-003`, `E-MARKUP-002`, `E-PARSE-001`).
- S117 — verified §34 currency directly in `compiler/SPEC.md` lines 15097-15660; extracted all 66 code+message pairs from the executed `.js` tier.
- S117 — classified all 66: 66 → (a) NEW §34 row, 0 → (b) map, 0 → (c) internal. Surfaced cross-unit item X1 — the 7 `class`/`try`/`throw` (a*) codes; statement-kind admission belongs to R1.
- S117 — wrote `M5-SWAP-S34-RECONCILIATION-PLAN.md`: enumeration + per-code classification tables + family-level recommendation (new `### 34.1` sub-section). STOP GATE reached.
- S117 — STOP GATE cleared. PA ratified the §34.1-sub-section approach.
- S117 EXEC — re-dispatched, worktree `agent-ac2436c9b2f020056`. Startup verification PASS: pwd under worktree, repo-root match, `git merge main` already-up-to-date (base == main HEAD ab1afe3c — R4 plan doc + brief on main). `bun install` + `bun run pretest` clean. Baseline `bun run test` = 18,173 pass / 0 fail / 169 skip / 1 todo / 739 files (R1 statement-bridge landed +71; matches re-dispatch brief; transient 2-fail flake on first post-install run cleared on briefed re-run).
- S117 EXEC — re-confirmed §34 spans `compiler/SPEC.md` 15097-15660; flat table ends 15657, `---` 15659, `## 35.` 15661. Re-enumerated 30 `E-EXPR-*` + 35 `E-STMT-*` + 1 `E-MARKUP-VALUE-UNCLOSED` = 66. Extracted verbatim message strings from `parse-expr.js`/`parse-stmt.js`.
- S117 EXEC — inserted `### 34.1 Native-Parser Parse Diagnostics` in `compiler/SPEC.md` before `## 35.` — normative prologue + three grouped sub-tables (30 `E-EXPR-*`, 35 `E-STMT-*`, 1 `E-MARKUP-VALUE-UNCLOSED` = 66 rows). +87 lines. §34.1 @ 15661, §35 shifted to 15748. Verbatim message strings from `parse-expr.js`/`parse-stmt.js`; `Section` column per the plan's construct-guard convention; 7 `class`/`try`/`throw` rows carry the R1 item-X1 cross-ref note. Committed.
- S117 EXEC — updated `compiler/SPEC-INDEX.md`: §34 row summary +S117 note; S117 substantive-landing bullet added; ran `bun run scripts/regen-spec-index.ts` — 24 Sections-table rows recomputed (§34 651L 15097-15747; §35+ shift +86). Committed.
- S117 EXEC — verification: pre-commit gate `bun test unit/integration/conformance --bail` 13,532 pass / 0 fail. Full `bun run test` 18,173 pass / 0 fail / 169 skip / 1 todo / 739 files — identical to baseline; zero new failures. No native-parser `.scrml`/`.js` touched (no renames) — `.scrml` predicate guard not triggered. R4 EXECUTION COMPLETE.
