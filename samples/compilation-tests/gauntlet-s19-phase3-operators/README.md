Phase 3 — Operators & Expressions fixtures.

One operator × one shape per fixture. Breadth first. See authoring report for coverage matrix.

All fixtures paired with `.expected.json` ({feature, shape, expectedOutcome, expectedCodes, notes}).
Author: scrml-developer agent (S19 Phase 3). Compilation is run by PA as an independent sweep.
