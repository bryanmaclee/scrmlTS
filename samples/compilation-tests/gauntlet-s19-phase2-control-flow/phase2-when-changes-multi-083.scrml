// when (@a, @b, @c) changes — multi-dep
<program>
    ${
        @query: string = ""
        @minPrice: number = 0
        @maxPrice: number = 100
        @page: number = 1
        when (@query, @minPrice, @maxPrice) changes {
            @page = 1
        }
    }
    <p>ok</>
</>
